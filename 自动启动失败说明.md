# ⚠️ 自动启动后端服务失败 - 需要手动操作

## 🔍 问题原因

由于网络代理配置问题，无法自动安装Python依赖包（fastapi、uvicorn等）。

## ✅ 解决方案

### 方法1：配置pip绕过代理（推荐）

**在新终端窗口执行：**

```bash
cd "/Users/zhangyang/Library/Mobile Documents/com~apple~CloudDocs/2. 领域（Areas）/17 学习AI 与 编程/Code/CloudunCollectionONE/backend"

# 激活虚拟环境
source venv/bin/activate

# 创建pip配置文件，禁用代理
mkdir -p ~/.pip
cat > ~/.pip/pip.conf << 'EOF'
[global]
proxy = 
EOF

# 或者直接设置环境变量
export http_proxy=""
export https_proxy=""
export HTTP_PROXY=""
export HTTPS_PROXY=""

# 安装依赖
pip install -r requirements.txt

# 启动服务
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 方法2：使用国内镜像源（如果网络慢）

```bash
cd "/Users/zhangyang/Library/Mobile Documents/com~apple~CloudDocs/2. 领域（Areas）/17 学习AI 与 编程/Code/CloudunCollectionONE/backend"

source venv/bin/activate

# 使用清华镜像源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 启动服务
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 方法3：检查是否已有其他Python环境

如果您之前成功运行过服务，可能依赖已经安装在系统Python中：

```bash
# 检查系统Python
python3 -c "import uvicorn, fastapi; print('✅ 系统Python有依赖')" && \
python3 -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

## 🎯 启动成功的标志

看到以下输出：
```
INFO:     Started server process [xxxxx]
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000
```

## ✅ 验证步骤

1. **访问API文档**：`http://localhost:8000/docs`
2. **搜索 `performance`**，应该能看到数据看板API
3. **刷新前端页面**，测试数据看板功能

---

**由于网络代理限制，无法自动安装依赖。请按照方法1手动配置并启动服务！**

