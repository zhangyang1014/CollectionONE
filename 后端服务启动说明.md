# 后端服务启动说明

## ⚠️ 当前情况

由于网络代理问题，无法自动安装依赖。但您的虚拟环境应该已经配置好了。

## 🚀 手动启动后端服务（推荐）

请在新终端窗口执行以下命令：

```bash
cd "/Users/zhangyang/Library/Mobile Documents/com~apple~CloudDocs/2. 领域（Areas）/17 学习AI 与 编程/Code/CloudunCollectionONE/backend"

# 方法1：使用start.sh脚本（会自动安装依赖）
bash start.sh

# 方法2：手动启动
source venv/bin/activate
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

## ✅ 启动成功的标志

看到以下输出说明启动成功：
```
INFO:     Started server process [xxxxx]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000
```

## 🔍 验证API

启动后，在浏览器访问：
- API文档：`http://localhost:8000/docs`
- 搜索 `performance`，应该能看到 `/api/v1/performance/collector/{collector_id}`

## 📝 如果启动失败

请检查：
1. 虚拟环境是否已激活
2. 依赖是否已安装（`pip list | grep fastapi`）
3. 是否有导入错误（查看启动日志）

---

**请在新终端窗口手动启动后端服务，然后刷新前端页面测试！**

