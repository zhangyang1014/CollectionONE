---
description: 后端API开发规则 - 避免404错误和端口混乱
tags: [backend, api, java, python, deprecated]
priority: critical
---

# 后端API开发规则

## 🚨 关键规则（违反将导致404错误）

### 1. 后端端口规则

```yaml
Java后端: 8080  # ✅ 当前使用
Python后端: 8000  # ❌ 已废弃，永远不要使用
```

### 2. 禁止使用Python后端

```bash
# ❌ 不要运行这些命令
cd backend
uvicorn app.main:app --port 8000

# ✅ 使用Java后端
cd backend-java
./start.sh
```

### 3. Java版本强制要求

```bash
# ✅ 必须使用Java 17
java -version  # 应该显示: openjdk version "17.0.17"

# ❌ 不要使用
java -version  # 如果显示 "25.0.1" 将导致编译失败
```

## 检查脚本

```bash
# 在每次提交前运行
./scripts/check-backend-rules.sh
```

## 违规检测

```bash
# 检查是否误用Python后端
ps aux | grep "uvicorn.*8000"  # 应该无结果
lsof -i :8000  # 应该无结果

# 检查Java后端是否运行
lsof -i :8080  # 应该有Java进程
```
