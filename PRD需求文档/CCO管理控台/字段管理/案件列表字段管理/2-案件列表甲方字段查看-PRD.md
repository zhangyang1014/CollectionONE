# 案件列表甲方字段查看 PRD

## 版本记录

| 版本号 | 日期 | 作者 | 变更说明 |
|--------|------|------|---------|
| v2.0.0 | 2025-12-08 | 产品大象 | 新增JSON模板下载、上传历史、版本对比功能 |
| v1.0.0 | 2025-12-07 | 产品大象 | 初始版本，基础查看和上传功能 |

---

## 1. 背景与目标
- 甲方通过上传字段JSON文件，供控台只读查看，便于对齐字段映射与展示。
- **区分场景**：本功能针对"案件列表"场景，与"案件详情甲方字段查看"区分开。
- 列表场景无需分组，直接平铺全部字段。
- 提供规范的JSON模板和上传管理功能，支持版本对比和切换。

## 2. 角色与范围
- 角色：CCO superadmin（可上传管理、版本切换）；甲方运营（只读查看）。
- 范围：控台"案件列表甲方字段查看"页面。
- 页面标题：**案件列表甲方字段查看**（区别于"案件详情甲方字段查看"）

## 3. 需求概述
- 读取接口 `/api/v1/tenants/{tenantId}/fields-json?scene=list` 返回的字段数组并直接展示。
- 若甲方未上传或返回空，则使用"案件列表标准字段管理"字段作为兜底mock展示。
- 不展示分组树；无编辑/排序/拖拽；纯只读表格。
- 提供JSON模板下载、上传、历史记录查看、版本对比、版本切换等完整功能。

## 4. 页面布局

### 4.1 页面头部
```text
┌───────────────────────────────────────────────────────────────┐
│  案件列表甲方字段查看                                              │
│                                                                │
│  当前甲方：XX金融 (tenant_001)                                   │
│  当前版本：版本3 | 上传时间：2025-12-07 15:30:00 | 字段数：14个     │
│                                                                │
│  [下载JSON模板]  [上传JSON文件]  [版本管理]                       │
└───────────────────────────────────────────────────────────────┘
```

### 4.2 按钮说明
1. **下载JSON模板**（次要按钮）：所有用户可见
2. **上传JSON文件**（主要按钮）：仅superadmin可见
3. **版本管理**（次要按钮）：仅superadmin可见，点击显示版本管理面板

## 5. 表格字段展示

### 5.1 表格列
- 序号
- 字段名称 `field_name`
- 字段标识 `field_key`
- 字段类型 `field_type`
- 枚举值（如Enum展示前2个并提示剩余数量，如"待分配, 催收中 等4个"）
- 是否必填 `is_required`（显示 ✓ 或 -）
- 排序 `sort_order`
- 描述 `description`（可选，鼠标悬停显示完整描述）

### 5.2 表格功能
- 支持按字段名称、字段标识搜索
- 支持按字段类型筛选
- 支持按是否必填筛选
- 每页显示20条，支持分页

## 6. JSON模板管理

### 6.1 JSON模板示例
提供标准JSON模板供甲方下载参考，模板包含所有必需字段：

```json
{
  "version": "1.0",
  "scene": "list",
  "tenant_id": "示例甲方ID",
  "tenant_name": "示例甲方名称",
  "updated_at": "2025-12-08T10:00:00Z",
  "description": "案件列表字段配置 - 用于案件列表页面展示",
  "fields": [
    {
      "field_name": "案件编号",
      "field_key": "case_id",
      "field_type": "String",
      "enum_values": null,
      "is_required": true,
      "sort_order": 1,
      "description": "案件唯一标识，不可为空"
    },
    {
      "field_name": "案件状态",
      "field_key": "case_status",
      "field_type": "Enum",
      "enum_values": ["待分配", "催收中", "已完成", "已关闭"],
      "is_required": true,
      "sort_order": 2,
      "description": "案件当前状态，用于流程跟踪"
    },
    {
      "field_name": "借款金额",
      "field_key": "loan_amount",
      "field_type": "Number",
      "enum_values": null,
      "is_required": true,
      "sort_order": 3,
      "description": "借款本金，单位：元"
    },
    {
      "field_name": "逾期天数",
      "field_key": "overdue_days",
      "field_type": "Integer",
      "enum_values": null,
      "is_required": false,
      "sort_order": 4,
      "description": "当前逾期天数，自动计算"
    },
    {
      "field_name": "借款人姓名",
      "field_key": "borrower_name",
      "field_type": "String",
      "enum_values": null,
      "is_required": true,
      "sort_order": 5,
      "description": "借款人真实姓名"
    },
    {
      "field_name": "借款人手机",
      "field_key": "borrower_phone",
      "field_type": "String",
      "enum_values": null,
      "is_required": true,
      "sort_order": 6,
      "description": "借款人联系电话"
    }
  ]
}
```text

### 6.2 字段类型说明
支持的字段类型（`field_type`）：
- `String`: 文本类型
- `Integer`: 整数类型
- `Number`: 数字类型（含小数）
- `Boolean`: 布尔类型
- `Date`: 日期类型（格式：YYYY-MM-DD）
- `DateTime`: 日期时间类型（格式：YYYY-MM-DD HH:mm:ss）
- `Enum`: 枚举类型（需配置 enum_values 数组）

### 6.3 模板下载功能
- 在页面右上角提供"下载JSON模板"按钮
- 按钮样式：次要按钮，图标为下载图标 📥
- 点击后下载文件名：`tenant_fields_list_template.json`
- 模板内容包含6个常用字段示例和详细说明

## 7. JSON上传功能

### 7.1 上传入口
- 在页面右上角提供"上传JSON文件"按钮（主要按钮）
- 按钮仅对 CCO superadmin 角色可见
- 点击后弹出上传弹窗

### 7.2 上传弹窗设计

#### 7.2.1 弹窗布局
```text
┌─────────────────────────────────────────────────────────────┐
│  上传案件列表甲方字段配置                                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  当前甲方信息                                                  │
│  ┌───────────────────────────────────────────────────┐      │
│  │ 甲方名称：XX金融                                      │      │
│  │ 甲方ID：tenant_001                                   │      │
│  │ 配置场景：案件列表                                     │      │
│  │ 当前生效版本：版本3                                    │      │
│  │ 当前字段数：14个                                       │      │
│  │ 最后更新：2025-12-07 15:30:00                        │      │
│  │ 更新人：管理员 (admin)                                │      │
│  └───────────────────────────────────────────────────┘      │
│                                                              │
│  上传历史记录（最近5次）                   [查看全部历史 >]      │
│  ┌───────────────────────────────────────────────────┐      │
│  │ ● 版本3 - 2025-12-07 15:30:00          [当前使用]  │      │
│  │   上传人：管理员 | 字段数：14 | [查看] [下载] [对比]  │      │
│  │                                                     │      │
│  │ ○ 版本2 - 2025-12-05 10:20:00                      │      │
│  │   上传人：管理员 | 字段数：12 | [查看] [下载] [对比] │      │
│  │                                                     │      │
│  │ ○ 版本1 - 2025-12-01 09:00:00                      │      │
│  │   上传人：管理员 | 字段数：10 | [查看] [下载] [对比] │      │
│  └───────────────────────────────────────────────────┘      │
│                                                              │
│  上传新文件                                                   │
│  ┌───────────────────────────────────────────────────┐      │
│  │         📤                                          │      │
│  │   [点击或拖拽上传JSON文件]                           │      │
│  │                                                     │      │
│  │   支持.json格式，文件大小不超过2MB                    │      │
│  │   上传后将创建新版本并自动设为当前使用版本             │      │
│  └───────────────────────────────────────────────────┘      │
│                                                              │
│  上传选项                                                     │
│  ☑ 上传前验证JSON格式                                         │
│  ☑ 上传成功后自动设为当前使用版本                              │
│  ☐ 上传后显示与上一版本的对比                                  │
│                                                              │
│                           [取消]  [确认上传]                  │
└─────────────────────────────────────────────────────────────┘
```

#### 7.2.2 弹窗功能说明

**当前甲方信息区域**：
- 显示甲方名称、ID
- 显示配置场景：案件列表（区分详情场景）
- 显示当前生效版本号
- 显示当前生效的字段总数
- 显示最后更新时间（精确到秒）
- 显示更新人信息

**上传历史记录区域**：
- 展示最近5次上传记录
- 当前使用的版本用实心圆 ● 标识，其他版本用空心圆 ○
- 每条记录显示：
  - 版本号、上传时间
  - 上传人、字段数量
  - 操作按钮：
    - **查看**：在弹窗中展示该版本的字段列表
    - **下载**：下载该版本的JSON文件
    - **对比**：与当前版本对比差异
- 点击"查看全部历史"进入版本管理页面

**文件上传区域**：
- 支持拖拽上传和点击选择
- 仅支持 `.json` 格式文件
- 文件大小限制：2MB
- 实时显示已选文件名和大小
- 提示：上传后将创建新版本

**上传选项**：
- ☑ 上传前验证JSON格式（默认勾选，建议保持）
- ☑ 上传成功后自动设为当前使用版本（默认勾选）
- ☐ 上传后显示与上一版本的对比（可选）

#### 7.2.3 上传后对比功能详解 ⭐

**功能说明**：
当用户勾选"上传后显示与上一版本的对比"选项后，文件上传成功会自动触发版本对比功能，帮助用户快速了解新旧版本的差异。

**触发时机**：
- 文件上传成功后
- 新版本已创建并（可选）设为当前使用版本
- 满足对比条件：系统中存在至少2个版本

**执行流程**：

```text
1. 用户点击"确认上传"
   ↓
2. 后端验证JSON格式（如果勾选验证选项）
   ↓
3. 创建新版本（如：版本4）
   ↓
4. 自动设为当前版本（如果勾选该选项）
   ↓
5. 检查是否勾选"显示对比"选项
   ↓
6. 【是】→ 自动调用对比接口
   - 基准版本：上一个版本（版本3）
   - 对比版本：新上传的版本（版本4）
   ↓
7. 关闭上传弹窗
   ↓
8. 自动弹出版本对比弹窗（全屏/90%宽度）
   ↓
9. 显示对比结果
```

**对比界面**：
自动弹出的对比弹窗与手动触发的对比功能完全一致（详见 8.3 章节），包括：

```text
┌─────────────────────────────────────────────────────────────────────────────────┐
│  自动版本对比：版本3 vs 版本4（新上传）                                      [×]  │
├─────────────────────────────────────────────────────────────────────────────────┤
│  版本3（上一版本）                    版本4（新上传，当前使用）                   │
│  2025-12-07 15:30:00 | 14个字段      2025-12-08 10:00:00 | 16个字段             │
│                                                                                  │
│  💡 提示：这是您刚刚上传的新版本与上一版本的对比结果                               │
│                                                                                  │
│  变更摘要：                                                                       │
│  • 新增字段：2个                                                                  │
│  • 删除字段：0个                                                                  │
│  • 修改字段：1个                                                                  │
│  • 未变更：13个                                                                   │
│                                                                                  │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                                                  │
│  📊 详细对比                                                                      │
│  （对比详情展示...与手动对比相同）                                                 │
│                                                                                  │
│          [导出对比报告]  [下载版本3]  [下载版本4]  [确认并关闭]                   │
└─────────────────────────────────────────────────────────────────────────────────┘
```

**对比内容**：
- ✅ 显示版本号、上传时间、字段数对比
- ✅ 变更摘要（新增、删除、修改、未变更统计）
- ✅ 新增字段列表（绿色标识）
- ✅ 删除字段列表（红色标识）
- ✅ 修改字段详情（黄色标识，逐个属性对比）
- ✅ 未变更字段列表（灰色标识，默认折叠）

**特殊标识**：
- 🆕 弹窗标题包含"（新上传）"标识
- 💡 顶部显示蓝色提示信息："这是您刚刚上传的新版本与上一版本的对比结果"
- ✅ 新版本默认高亮显示（当前使用）

**用户操作**：
1. **查看对比结果**：
   - 浏览新增、删除、修改的字段
   - 展开/折叠未变更字段
   - 滚动查看完整对比

2. **导出对比报告**：
   - 生成Markdown格式报告
   - 文件名：`version_compare_v3_vs_v4_新上传_20251208100000.md`
   - 自动包含对比时间和"新上传"标识

3. **下载版本文件**：
   - 下载上一版本（版本3）
   - 下载新上传版本（版本4）

4. **关闭对比窗口**：
   - 点击"确认并关闭"或右上角 ×
   - 返回字段列表页面（已刷新为新版本）

**异常处理**：

| 异常情况 | 处理方式 |
|---------|---------|
| 仅有1个版本（首次上传） | 不触发对比，显示提示："这是首次上传，无历史版本可对比" |
| 上一版本数据缺失 | 显示错误："无法获取上一版本数据，对比失败" |
| 对比接口调用失败 | 显示错误提示，但不影响上传成功的结果 |
| 对比计算超时（>5秒） | 显示加载中状态，5秒后提示"对比计算中，请稍后手动查看" |
| 用户取消上传弹窗 | 如果上传已完成，仍然触发对比（后台执行） |

**最佳实践建议**：

✅ **建议勾选的场景**：
- 对现有配置进行调整（新增、删除、修改字段）
- 需要向甲方汇报配置变更内容
- 多人协作，需要了解他人的修改
- 正式环境上线前的最终确认

❌ **不建议勾选的场景**：
- 首次上传（无历史版本可对比）
- 仅修改字段描述等次要信息
- 频繁测试上传
- 网络环境不稳定时

**性能要求**：
- 对比计算时间：< 1秒（100个字段以内）
- 对比弹窗加载时间：< 500ms
- 报告导出时间：< 2秒

**数据埋点**：
- 上传时勾选对比选项的比例
- 对比功能使用频率
- 对比报告导出次数
- 对比窗口停留时长

### 7.3 上传验证规则

#### 7.3.1 文件格式验证
- 文件扩展名必须为 `.json`
- 文件大小不超过 2MB
- JSON格式必须合法（可解析）

#### 7.3.2 JSON内容验证
必需字段检查：
- `fields` 数组必须存在且非空
- 每个字段对象必须包含：
  - `field_name`（字符串，非空，长度1-50字符）
  - `field_key`（字符串，非空，符合变量命名规范：字母开头，仅包含字母数字下划线）
  - `field_type`（字符串，必须是支持的类型之一）
  - `is_required`（布尔值）
  - `sort_order`（整数，大于0）

可选字段：
- `enum_values`（数组，当 field_type 为 Enum 时必需）
- `description`（字符串，长度不超过200字符）

#### 7.3.3 业务规则验证
- `field_key` 在同一JSON中不能重复
- `sort_order` 建议不重复（可警告但不阻止）
- 必填字段（`is_required: true`）至少应有1个
- Enum类型的 `enum_values` 数组至少包含2个元素

#### 7.3.4 验证失败提示
```typescript
验证失败示例：
❌ 第3个字段缺少必需属性 field_key
❌ 第5个字段的 field_type "Text" 不在支持类型列表中
   支持的类型：String, Integer, Number, Boolean, Date, DateTime, Enum
❌ 第8个字段的 field_type 为 Enum，但缺少 enum_values 或值少于2个
❌ 字段标识 "case_id" 重复出现（第2和第7个字段）
❌ 第10个字段的 field_key "2case" 格式不正确，必须以字母开头
```

## 8. 版本管理功能 ⭐ 核心功能

### 8.1 版本管理面板
点击"版本管理"按钮，在页面右侧弹出抽屉式面板：

```text
┌─────────────────────────────────────────────────────────────┐
│  版本管理 - 案件列表字段配置                          [×]      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  当前使用版本：版本3                                           │
│  ┌─────────────────────────────────────────────────┐        │
│  │ 搜索版本：[___________] 🔍                       │        │
│  │ 筛选：[全部版本 ▼] [全部上传人 ▼]                │        │
│  └─────────────────────────────────────────────────┘        │
│                                                              │
│  ┌─────────────────────────────────────────────────┐        │
│  │ ● 版本3                              [当前使用]  │        │
│  │   上传时间：2025-12-07 15:30:00                  │        │
│  │   上传人：管理员 (admin)                          │        │
│  │   字段数：14个                                    │        │
│  │   说明：新增借款人联系信息字段                     │        │
│  │                                                   │        │
│  │   [查看详情] [下载JSON] [与上一版本对比]           │        │
│  └─────────────────────────────────────────────────┘        │
│                                                              │
│  ┌─────────────────────────────────────────────────┐        │
│  │ ○ 版本2                                          │        │
│  │   上传时间：2025-12-05 10:20:00                  │        │
│  │   上传人：管理员 (admin)                          │        │
│  │   字段数：12个                                    │        │
│  │   说明：调整字段顺序，新增逾期天数                 │        │
│  │                                                   │        │
│  │   [查看详情] [下载JSON] [设为当前版本] [对比]      │        │
│  └─────────────────────────────────────────────────┘        │
│                                                              │
│  ┌─────────────────────────────────────────────────┐        │
│  │ ○ 版本1                                          │        │
│  │   上传时间：2025-12-01 09:00:00                  │        │
│  │   上传人：管理员 (admin)                          │        │
│  │   字段数：10个                                    │        │
│  │   说明：初始版本                                  │        │
│  │                                                   │        │
│  │   [查看详情] [下载JSON] [设为当前版本] [对比]      │        │
│  └─────────────────────────────────────────────────┘        │
│                                                              │
│  显示 1-10 / 共25条                      [上一页] [下一页]   │
└─────────────────────────────────────────────────────────────┘
```

### 8.2 版本操作功能

#### 8.2.1 查看详情
点击"查看详情"按钮，在弹窗中展示该版本的完整字段列表：

```text
┌─────────────────────────────────────────────────────────────┐
│  版本详情 - 版本2                                      [×]    │
├─────────────────────────────────────────────────────────────┤
│  上传时间：2025-12-05 10:20:00                               │
│  上传人：管理员 (admin)                                       │
│  字段数：12个                                                 │
│  说明：调整字段顺序，新增逾期天数                              │
│                                                              │
│  ┌───────────────────────────────────────────────┐          │
│  │ 序号 | 字段名称 | 字段标识 | 类型 | 必填 | 排序 │          │
│  ├───────────────────────────────────────────────┤          │
│  │  1  | 案件编号 | case_id  | String | ✓ |  1   │          │
│  │  2  | 案件状态 | case_status | Enum | ✓ | 2   │          │
│  │  3  | 借款金额 | loan_amount | Number | ✓ | 3 │          │
│  │ ...  | ...      | ...       | ...   | ... | ...│          │
│  └───────────────────────────────────────────────┘          │
│                                                              │
│                    [下载此版本] [关闭]                         │
└─────────────────────────────────────────────────────────────┘
```

#### 8.2.2 下载JSON
- 点击"下载JSON"直接下载该版本的JSON文件
- 文件名格式：`tenant_{tenantId}_list_v{version}_{timestamp}.json`
- 示例：`tenant_001_list_v2_20251205.json`

#### 8.2.3 设为当前版本
- 点击"设为当前版本"按钮（仅非当前版本显示）
- 弹出二次确认弹窗：

```text
┌───────────────────────────────────────────┐
│  确认切换版本                              │
├───────────────────────────────────────────┤
│                                            │
│  您确定要将版本2设为当前使用版本吗？        │
│                                            │
│  当前版本：版本3（14个字段）                │
│  目标版本：版本2（12个字段）                │
│                                            │
│  切换后，字段映射配置将使用版本2的字段。    │
│                                            │
│     [取消]            [确认切换]           │
└───────────────────────────────────────────┘
```

- 确认后：
  1. 更新数据库：将版本2的 `is_active` 设为 `true`
  2. 将版本3的 `is_active` 设为 `false`
  3. 刷新页面显示版本2的字段
  4. 显示成功提示：`已切换到版本2（12个字段）`

### 8.3 版本对比功能 ⭐⭐ 重点功能

#### 8.3.1 对比触发方式
1. 在版本管理面板点击"与上一版本对比"
2. 在版本管理面板点击"对比"，选择要对比的版本

#### 8.3.2 对比结果展示
弹出对比结果弹窗：

```text
┌─────────────────────────────────────────────────────────────────────────────────┐
│  版本对比：版本2 vs 版本3                                                   [×]  │
├─────────────────────────────────────────────────────────────────────────────────┤
│  版本2（基准版本）                    版本3（当前版本）                           │
│  2025-12-05 10:20:00 | 12个字段      2025-12-07 15:30:00 | 14个字段             │
│                                                                                  │
│  变更摘要：                                                                       │
│  • 新增字段：2个                                                                  │
│  • 删除字段：0个                                                                  │
│  • 修改字段：3个                                                                  │
│                                                                                  │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                                                  │
│  📊 详细对比                                                                      │
│                                                                                  │
│  ┌────────────────────────────────────────────────────────────────────┐        │
│  │ 🟢 新增字段（2个）                                                     │        │
│  ├────────────────────────────────────────────────────────────────────┤        │
│  │ + 借款人姓名 (borrower_name)                                          │        │
│  │   类型：String | 必填：是 | 排序：5                                   │        │
│  │                                                                       │        │
│  │ + 借款人手机 (borrower_phone)                                         │        │
│  │   类型：String | 必填：是 | 排序：6                                   │        │
│  └────────────────────────────────────────────────────────────────────┘        │
│                                                                                  │
│  ┌────────────────────────────────────────────────────────────────────┐        │
│  │ 🔴 删除字段（0个）                                                     │        │
│  ├────────────────────────────────────────────────────────────────────┤        │
│  │ 无删除字段                                                             │        │
│  └────────────────────────────────────────────────────────────────────┘        │
│                                                                                  │
│  ┌────────────────────────────────────────────────────────────────────┐        │
│  │ 🟡 修改字段（3个）                                                     │        │
│  ├────────────────────────────────────────────────────────────────────┤        │
│  │ ≈ 案件状态 (case_status)                                              │        │
│  │   枚举值：                                                             │        │
│  │   - 版本2：["待分配", "催收中", "已完成"]                              │        │
│  │   + 版本3：["待分配", "催收中", "已完成", "已关闭"]                    │        │
│  │                                                                       │        │
│  │ ≈ 逾期天数 (overdue_days)                                             │        │
│  │   排序：                                                               │        │
│  │   - 版本2：4                                                          │        │
│  │   + 版本3：7                                                          │        │
│  │                                                                       │        │
│  │ ≈ 借款金额 (loan_amount)                                              │        │
│  │   描述：                                                               │        │
│  │   - 版本2：借款本金                                                    │        │
│  │   + 版本3：借款本金，单位：元                                          │        │
│  └────────────────────────────────────────────────────────────────────┘        │
│                                                                                  │
│  ┌────────────────────────────────────────────────────────────────────┐        │
│  │ ⚪ 未变更字段（9个）                                      [展开/收起]  │        │
│  └────────────────────────────────────────────────────────────────────┘        │
│                                                                                  │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                                                  │
│  选择要使用的版本：                                                               │
│  ○ 版本2（12个字段）  ● 版本3（14个字段，当前使用）                               │
│                                                                                  │
│          [导出对比报告]  [下载版本2]  [下载版本3]  [关闭]                         │
└─────────────────────────────────────────────────────────────────────────────────┘
```text

#### 8.3.3 对比结果说明

**变更类型标识**：
- 🟢 绿色：新增字段
- 🔴 红色：删除字段
- 🟡 黄色：修改字段
- ⚪ 灰色：未变更字段

**变更详情**：
- **新增字段**：显示字段完整信息（名称、标识、类型、枚举值、必填、排序）
- **删除字段**：显示被删除的字段信息
- **修改字段**：仅显示发生变化的属性，用 `-` 和 `+` 标识前后差异

**对比维度**：
- field_name（字段名称）
- field_type（字段类型）
- enum_values（枚举值）
- is_required（是否必填）
- sort_order（排序）
- description（描述）

**版本选择**：
- 在对比结果底部提供版本选择单选框
- 默认选中当前使用版本
- 可以直接切换到另一个版本
- 切换后立即生效

#### 8.3.4 导出对比报告
点击"导出对比报告"按钮，生成Markdown格式的对比报告文件：

```markdown
# 版本对比报告

## 基本信息
- 对比版本：版本2 vs 版本3
- 对比时间：2025-12-08 10:30:00
- 操作人：管理员 (admin)

## 版本信息
| 项目 | 版本2 | 版本3 |
|------|-------|-------|
| 上传时间 | 2025-12-05 10:20:00 | 2025-12-07 15:30:00 |
| 字段数 | 12个 | 14个 |
| 上传人 | 管理员 | 管理员 |

## 变更摘要
- 新增字段：2个
- 删除字段：0个
- 修改字段：3个
- 未变更：9个

## 详细对比
### 新增字段
1. 借款人姓名 (borrower_name)
   - 类型：String
   - 必填：是
   - 排序：5

2. 借款人手机 (borrower_phone)
   - 类型：String
   - 必填：是
   - 排序：6

### 修改字段
1. 案件状态 (case_status)
   - 枚举值：["待分配", "催收中", "已完成"] → ["待分配", "催收中", "已完成", "已关闭"]

2. 逾期天数 (overdue_days)
   - 排序：4 → 7

3. 借款金额 (loan_amount)
   - 描述：借款本金 → 借款本金，单位：元

---
生成时间：2025-12-08 10:30:00
```

文件名格式：`version_compare_{v2}_vs_{v3}_{timestamp}.md`

## 9. 上传记录留档

### 9.1 数据库表设计

#### tenant_field_uploads 表
```sql
CREATE TABLE tenant_field_uploads (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
  tenant_id VARCHAR(64) NOT NULL COMMENT '甲方ID',
  scene VARCHAR(20) NOT NULL DEFAULT 'list' COMMENT '场景：list-案件列表, detail-案件详情',
  version INT NOT NULL COMMENT '版本号，按甲方+场景自增',
  file_name VARCHAR(255) NOT NULL COMMENT '原始文件名',
  file_size INT NOT NULL COMMENT '文件大小（字节）',
  file_path VARCHAR(512) NOT NULL COMMENT '存储路径',
  fields_count INT NOT NULL COMMENT '字段数量',
  uploaded_by VARCHAR(64) NOT NULL COMMENT '上传人ID',
  uploaded_by_name VARCHAR(100) COMMENT '上传人姓名',
  uploaded_at DATETIME NOT NULL COMMENT '上传时间',
  json_content JSON NOT NULL COMMENT 'JSON文件内容',
  is_active BOOLEAN DEFAULT FALSE COMMENT '是否为当前生效版本',
  version_note VARCHAR(500) COMMENT '版本说明（可选）',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  INDEX idx_tenant_scene (tenant_id, scene),
  INDEX idx_tenant_scene_version (tenant_id, scene, version),
  INDEX idx_tenant_scene_active (tenant_id, scene, is_active),
  INDEX idx_uploaded_at (uploaded_at),

  UNIQUE KEY uk_tenant_scene_version (tenant_id, scene, version)
) COMMENT '甲方字段上传记录表';
```

### 9.2 文件存储规则
- 存储路径：`/uploads/tenant-fields/{tenant_id}/{scene}/v{version}/fields.json`
- 示例：`/uploads/tenant-fields/tenant_001/list/v3/fields.json`
- 同时在数据库 `json_content` 字段保存JSON内容（便于查询和对比）

### 9.3 版本管理规则
- 每次上传创建新版本记录
- 版本号按甲方+场景维度自增：1, 2, 3, ...
- 同一甲方的 list 和 detail 场景版本号独立计算
- 新上传的版本自动设为当前生效版本（`is_active = true`）
- 旧版本的 `is_active` 自动设为 `false`
- 保留所有历史版本，不删除

### 9.4 上传成功后操作
1. 保存JSON文件到文件系统
2. 插入上传记录到数据库
3. 更新该甲方该场景的当前生效版本标识
4. 刷新页面显示最新数据
5. 显示成功提示：`成功上传版本{version}，共 {count} 个字段`

## 10. 前端要求

### 10.1 页面路由与标题
- 路由：`/field-config/tenant-fields-list`（区别于详情场景）
- 页面标题：**案件列表甲方字段查看**
- 面包屑：字段管理 / 案件列表甲方字段查看

### 10.2 页面布局
- 去掉分组树、拖拽等控件，保持平铺表格
- 表格列：序号、字段名称、字段标识、字段类型、枚举值、是否必填、排序、描述
- 枚举值列：如果是Enum类型且值超过2个，显示前2个 + "等{n}个"
- 是否必填列：显示 ✓（必填）或 -（非必填）
- 支持表格搜索（按字段名称、字段标识）

### 10.3 数据加载逻辑
```typescript
// 伪代码
async function loadTenantFields() {
  try {
    const response = await fetch(`/api/v1/tenants/${tenantId}/fields-json?scene=list`)
    if (response.ok && response.data.fields.length > 0) {
      // 显示甲方上传的字段
      displayFields(response.data.fields)
      showCurrentVersion({
        version: response.data.version,
        uploadedAt: response.data.fetched_at,
        fieldsCount: response.data.fields_count
      })
    } else {
      // 兜底：加载标准字段
      const standardFields = await fetch('/api/v1/standard-fields/case-list')
      displayFields(standardFields)
      showEmptyTip('当前甲方未上传字段配置，显示标准字段作为参考')
    }
  } catch (error) {
    handleError(error)
  }
}
```

### 10.4 版本对比逻辑
```typescript
// 版本对比算法
interface FieldComparison {
  added: Field[]       // 新增字段
  removed: Field[]     // 删除字段
  modified: FieldDiff[] // 修改字段
  unchanged: Field[]   // 未变更字段
}

function compareVersions(v1: Version, v2: Version): FieldComparison {
  const v1Map = new Map(v1.fields.map(f => [f.field_key, f]))
  const v2Map = new Map(v2.fields.map(f => [f.field_key, f]))

  const result: FieldComparison = {
    added: [],
    removed: [],
    modified: [],
    unchanged: []
  }

  // 查找新增和修改
  for (const [key, v2Field] of v2Map) {
    if (!v1Map.has(key)) {
      result.added.push(v2Field)
    } else {
      const v1Field = v1Map.get(key)
      const diff = compareFields(v1Field, v2Field)
      if (diff.hasChanges) {
        result.modified.push({ field: v2Field, changes: diff.changes })
      } else {
        result.unchanged.push(v2Field)
      }
    }
  }

  // 查找删除
  for (const [key, v1Field] of v1Map) {
    if (!v2Map.has(key)) {
      result.removed.push(v1Field)
    }
  }

  return result
}

function compareFields(f1: Field, f2: Field) {
  const changes = []

  if (f1.field_name !== f2.field_name) {
    changes.push({ property: 'field_name', old: f1.field_name, new: f2.field_name })
  }
  if (f1.field_type !== f2.field_type) {
    changes.push({ property: 'field_type', old: f1.field_type, new: f2.field_type })
  }
  if (JSON.stringify(f1.enum_values) !== JSON.stringify(f2.enum_values)) {
    changes.push({ property: 'enum_values', old: f1.enum_values, new: f2.enum_values })
  }
  if (f1.is_required !== f2.is_required) {
    changes.push({ property: 'is_required', old: f1.is_required, new: f2.is_required })
  }
  if (f1.sort_order !== f2.sort_order) {
    changes.push({ property: 'sort_order', old: f1.sort_order, new: f2.sort_order })
  }
  if (f1.description !== f2.description) {
    changes.push({ property: 'description', old: f1.description, new: f2.description })
  }

  return {
    hasChanges: changes.length > 0,
    changes
  }
}
```typescript

### 10.5 上传功能前端实现

#### 10.5.1 基础上传功能
- 使用 Element Plus 的 `el-upload` 组件
- 支持拖拽上传
- 上传前进行本地验证（JSON格式、文件大小）
- 显示上传进度
- 上传成功后自动刷新表格数据

#### 10.5.2 上传后对比功能实现 ⭐

**前端实现流程**：

```typescript
// 上传确认处理函数
async function handleConfirmUpload() {
  const uploadOptions = {
    validate: validateCheckbox.value,           // 上传前验证
    setActive: setActiveCheckbox.value,         // 设为当前版本
    showCompare: showCompareCheckbox.value      // 显示对比
  }

  try {
    uploadLoading.value = true

    // 1. 上传文件
    const formData = new FormData()
    formData.append('file', selectedFile.value)
    formData.append('scene', 'list')
    formData.append('uploadedBy', currentUser.id)

    const uploadResult = await uploadFieldsJson(tenantId, formData)

    // 2. 获取新版本号
    const newVersion = uploadResult.version

    ElMessage.success(`成功上传版本${newVersion}，共 ${uploadResult.fields_count} 个字段`)

    // 3. 关闭上传弹窗
    uploadDialogVisible.value = false

    // 4. 刷新字段列表
    await loadTenantFields()

    // 5. 判断是否需要显示对比
    if (uploadOptions.showCompare && newVersion > 1) {
      // 等待200ms让用户看到上传成功提示
      await new Promise(resolve => setTimeout(resolve, 200))

      // 自动触发版本对比
      await showAutoCompareAfterUpload(newVersion)
    } else if (newVersion === 1) {
      // 首次上传，提示无法对比
      ElMessage.info('这是首次上传，无历史版本可对比')
    }

  } catch (error) {
    ElMessage.error('上传失败：' + error.message)
  } finally {
    uploadLoading.value = false
  }
}

// 上传后自动对比函数
async function showAutoCompareAfterUpload(newVersion: number) {
  try {
    const previousVersion = newVersion - 1

    // 调用对比接口
    const compareResult = await compareFieldsJsonVersions(
      tenantId,
      previousVersion,  // 上一版本
      newVersion,       // 新上传的版本
      'list'
    )

    // 设置对比数据
    compareData.value = compareResult

    // 标记为自动对比（用于UI显示）
    isAutoCompare.value = true

    // 显示对比弹窗
    compareDialogVisible.value = true

  } catch (error) {
    console.error('自动对比失败：', error)
    // 对比失败不影响上传成功的结果，只显示警告
    ElMessage.warning('无法自动对比版本，您可稍后手动查看')
  }
}
```typescript

**状态管理**：

```typescript
// 上传选项状态
const uploadOptions = reactive({
  validate: true,      // 上传前验证（默认勾选）
  setActive: true,     // 设为当前版本（默认勾选）
  showCompare: false   // 显示对比（默认不勾选）
})

// 对比弹窗状态
const compareDialogVisible = ref(false)
const compareData = ref(null)
const isAutoCompare = ref(false)  // 标识是否为上传后自动对比
```

**对比弹窗UI差异**：

```vue
<!-- 对比弹窗标题 -->
<template>
  <el-dialog
    v-model="compareDialogVisible"
    :title="dialogTitle"
    width="90%"
    fullscreen
  >
    <!-- 自动对比提示 -->
    <el-alert
      v-if="isAutoCompare"
      title="💡 这是您刚刚上传的新版本与上一版本的对比结果"
      type="info"
      :closable="false"
      show-icon
      style="margin-bottom: 20px"
    />

    <!-- 对比内容 -->
    <version-compare-content :data="compareData" />

    <template #footer>
      <el-button @click="exportCompareReport">导出对比报告</el-button>
      <el-button @click="downloadVersion(compareData.version1.version)">
        下载版本{{ compareData.version1.version }}
      </el-button>
      <el-button @click="downloadVersion(compareData.version2.version)">
        下载版本{{ compareData.version2.version }}
      </el-button>
      <el-button type="primary" @click="closeCompareDialog">
        {{ isAutoCompare ? '确认并关闭' : '关闭' }}
      </el-button>
    </template>
  </el-dialog>
</template>

<script setup>
const dialogTitle = computed(() => {
  if (!compareData.value) return '版本对比'

  const v1 = compareData.value.version1.version
  const v2 = compareData.value.version2.version

  if (isAutoCompare.value) {
    return `自动版本对比：版本${v1} vs 版本${v2}（新上传）`
  } else {
    return `版本对比：版本${v1} vs 版本${v2}`
  }
})

function closeCompareDialog() {
  compareDialogVisible.value = false
  isAutoCompare.value = false  // 重置自动对比标识
}
</script>
```text

**错误处理**：

```typescript
// 对比接口调用失败处理
async function showAutoCompareAfterUpload(newVersion: number) {
  const previousVersion = newVersion - 1

  try {
    // 设置超时
    const timeoutPromise = new Promise((_, reject) =>
      setTimeout(() => reject(new Error('对比超时')), 5000)
    )

    const comparePromise = compareFieldsJsonVersions(
      tenantId,
      previousVersion,
      newVersion,
      'list'
    )

    // 5秒超时保护
    const compareResult = await Promise.race([comparePromise, timeoutPromise])

    compareData.value = compareResult
    isAutoCompare.value = true
    compareDialogVisible.value = true

  } catch (error) {
    if (error.message === '对比超时') {
      ElMessage.warning('对比计算中，请稍后在版本管理中手动查看')
    } else if (error.response?.status === 404) {
      ElMessage.warning('上一版本数据不存在，无法对比')
    } else {
      ElMessage.warning('自动对比失败，您可在版本管理中手动查看')
    }

    // 记录错误日志
    console.error('自动对比错误：', {
      newVersion,
      previousVersion,
      error: error.message
    })
  }
}
```

**性能优化**：

1. **异步加载**：对比计算在后台进行，不阻塞UI
2. **缓存优化**：对比结果临时缓存，重复打开不重新计算
3. **懒加载**：未变更字段默认折叠，点击时才渲染
4. **虚拟滚动**：超过100个字段时使用虚拟滚动

**用户体验优化**：

1. **提示信息**：
   - 上传成功立即显示Toast提示
   - 对比弹窗自动弹出（200ms延迟，让用户看到成功提示）
   - 对比弹窗顶部显示蓝色提示条

2. **视觉标识**：
   - 自动对比弹窗标题包含"（新上传）"
   - 新版本卡片高亮显示
   - 底部按钮文案改为"确认并关闭"

3. **操作便捷**：
   - 一键导出对比报告（自动包含"新上传"标识）
   - 快速下载任一版本的JSON文件
   - ESC键可关闭对比弹窗

## 11. 后端要求

### 11.1 现有接口优化
**GET `/api/v1/tenants/{tenantId}/fields-json`**

请求参数：
- `scene`: 场景，可选值：`list`（案件列表）、`detail`（案件详情）

返回格式：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "tenant_id": "tenant_001",
    "tenant_name": "XX金融",
    "scene": "list",
    "version": 3,
    "fetched_at": "2025-12-07T15:30:00Z",
    "fields_count": 14,
    "uploaded_by": "admin",
    "uploaded_by_name": "管理员",
    "fields": [
      {
        "field_name": "案件编号",
        "field_key": "case_id",
        "field_type": "String",
        "enum_values": null,
        "is_required": true,
        "sort_order": 1,
        "description": "案件唯一标识"
      }
      // ... 更多字段
    ]
  }
}
```

### 11.2 新增接口

#### 11.2.1 上传JSON文件
**POST `/api/v1/tenants/{tenantId}/fields-json/upload`**

请求：
- Content-Type: `multipart/form-data`
- 参数：
  - `file`: JSON文件
  - `scene`: 场景（list 或 detail）
  - `uploaded_by`: 上传人ID
  - `version_note`: 版本说明（可选）

响应：
```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "version": 4,
    "fields_count": 15,
    "uploaded_at": "2025-12-08T10:30:00Z",
    "is_active": true
  }
}
```

#### 11.2.2 获取上传历史
**GET `/api/v1/tenants/{tenantId}/fields-json/history`**

请求参数：
- `scene`: 场景（list 或 detail）
- `page`: 页码（默认1）
- `page_size`: 每页数量（默认10，最大50）

响应：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 25,
    "current_version": 3,
    "records": [
      {
        "version": 3,
        "file_name": "fields_v3.json",
        "file_size": 12345,
        "fields_count": 14,
        "uploaded_by": "admin",
        "uploaded_by_name": "管理员",
        "uploaded_at": "2025-12-07T15:30:00Z",
        "is_active": true,
        "version_note": "新增借款人联系信息字段"
      }
      // ... 更多记录
    ]
  }
}
```

#### 11.2.3 获取特定版本详情
**GET `/api/v1/tenants/{tenantId}/fields-json/version/{version}`**

请求参数：
- `scene`: 场景（list 或 detail）

响应：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "version": 2,
    "file_name": "fields_v2.json",
    "fields_count": 12,
    "uploaded_by": "admin",
    "uploaded_by_name": "管理员",
    "uploaded_at": "2025-12-05T10:20:00Z",
    "is_active": false,
    "version_note": "调整字段顺序",
    "fields": [
      // ... 字段列表
    ]
  }
}
```

#### 11.2.4 版本对比
**GET `/api/v1/tenants/{tenantId}/fields-json/compare`**

请求参数：
- `scene`: 场景（list 或 detail）
- `version1`: 基准版本号
- `version2`: 对比版本号

响应：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "version1": {
      "version": 2,
      "uploaded_at": "2025-12-05T10:20:00Z",
      "fields_count": 12
    },
    "version2": {
      "version": 3,
      "uploaded_at": "2025-12-07T15:30:00Z",
      "fields_count": 14
    },
    "summary": {
      "added": 2,
      "removed": 0,
      "modified": 3,
      "unchanged": 9
    },
    "details": {
      "added": [
        {
          "field_name": "借款人姓名",
          "field_key": "borrower_name",
          "field_type": "String",
          "is_required": true,
          "sort_order": 5
        }
        // ... 更多新增字段
      ],
      "removed": [],
      "modified": [
        {
          "field_key": "case_status",
          "field_name": "案件状态",
          "changes": [
            {
              "property": "enum_values",
              "old_value": ["待分配", "催收中", "已完成"],
              "new_value": ["待分配", "催收中", "已完成", "已关闭"]
            }
          ]
        }
        // ... 更多修改字段
      ],
      "unchanged": [
        // ... 未变更字段列表（可选返回）
      ]
    }
  }
}
```text

#### 11.2.5 设置当前版本
**PUT `/api/v1/tenants/{tenantId}/fields-json/activate/{version}`**

请求参数：
- `scene`: 场景（list 或 detail）

请求体：
```json
{
  "operator_id": "admin",
  "reason": "切换到历史稳定版本"
}
```

响应：
```json
{
  "code": 200,
  "message": "版本切换成功",
  "data": {
    "old_version": 3,
    "new_version": 2,
    "activated_at": "2025-12-08T11:00:00Z"
  }
}
```

#### 11.2.6 下载历史版本
**GET `/api/v1/tenants/{tenantId}/fields-json/download/{version}`**

请求参数：
- `scene`: 场景（list 或 detail）

响应：
- Content-Type: `application/json`
- Content-Disposition: `attachment; filename="tenant_{tenantId}_{scene}_v{version}.json"`
- Body: JSON文件内容

#### 11.2.7 下载JSON模板
**GET `/api/v1/tenants/fields-json/template`**

请求参数：
- `scene`: 场景（list 或 detail），默认 list

响应：
- Content-Type: `application/json`
- Content-Disposition: `attachment; filename="tenant_fields_{scene}_template.json"`
- Body: 标准模板JSON（包含示例数据）

### 11.3 后端验证逻辑
- 实现完整的JSON格式验证（参见 7.3 章节）
- 验证失败返回详细错误信息（指明哪个字段有问题）
- 文件大小限制：2MB
- 事务处理：确保文件保存和数据库记录一致性
- 版本切换时检查版本是否存在

### 11.4 权限控制
- 上传功能仅限 CCO superadmin
- 版本切换仅限 CCO superadmin
- 查看功能对所有角色开放
- API需要检查用户权限

## 12. 验收标准

### 12.1 JSON模板功能
- ✅ 能下载JSON模板文件
- ✅ 模板包含完整的字段示例和说明
- ✅ 模板格式与实际上传要求一致
- ✅ 模板文件名包含场景标识（list）

### 12.2 上传弹窗功能
- ✅ 弹窗标题包含"案件列表"场景标识
- ✅ 弹窗正确显示当前甲方信息（名称、ID、场景、版本、字段数、最后更新时间、更新人）
- ✅ 弹窗正确显示最近5次上传历史记录
- ✅ 当前使用版本用实心圆标识
- ✅ 支持拖拽和点击选择文件
- ✅ 文件选择后显示文件名和大小

### 12.3 上传验证功能
- ✅ 正确验证文件格式（仅支持.json）
- ✅ 正确验证文件大小（不超过2MB）
- ✅ 正确验证JSON格式合法性
- ✅ 正确验证必需字段完整性
- ✅ 正确验证字段类型有效性
- ✅ 正确验证field_key命名规范
- ✅ 正确验证Enum类型的enum_values
- ✅ 验证失败时显示详细错误提示

### 12.4 上传记录留档功能
- ✅ 每次上传在数据库创建新记录
- ✅ 每次上传保存JSON文件到文件系统
- ✅ 版本号按甲方+场景维度正确自增
- ✅ 新版本自动设为当前生效版本
- ✅ 旧版本正确标记为非生效
- ✅ 能查询上传历史记录
- ✅ 能下载历史版本的JSON文件

### 12.5 版本管理功能
- ✅ 版本管理面板正确显示所有版本
- ✅ 当前使用版本正确标识
- ✅ 能查看任意版本的详情
- ✅ 能下载任意版本的JSON文件
- ✅ 能切换到历史版本
- ✅ 版本切换需要二次确认
- ✅ 版本切换后页面自动刷新

### 12.6 版本对比功能 ⭐ 重点验收
- ✅ 能对比任意两个版本
- ✅ 正确识别新增字段
- ✅ 正确识别删除字段
- ✅ 正确识别修改字段（包括所有属性变化）
- ✅ 正确统计未变更字段
- ✅ 对比结果用不同颜色标识
- ✅ 修改字段显示前后差异（- 和 +）
- ✅ 能在对比结果中选择要使用的版本
- ✅ 能导出对比报告（Markdown格式）
- ✅ 对比报告包含完整的变更信息

### 12.7 数据展示功能
- ✅ 页面标题为"案件列表甲方字段查看"
- ✅ 有数据：表格正常显示甲方上传的全部字段，无分组筛选
- ✅ 无数据：表格显示14条标准字段兜底，必填/排序与标准一致
- ✅ 页面无新增/编辑/删除/拖拽入口（仅查看）
- ✅ 枚举值超过2个时正确显示"等n个"
- ✅ 显示当前版本信息
- ✅ 支持字段搜索和筛选

### 12.8 权限控制
- ✅ superadmin能看到"上传JSON文件"按钮
- ✅ superadmin能看到"版本管理"按钮
- ✅ 普通用户看不到上传和版本管理按钮
- ✅ 所有用户都能下载模板
- ✅ 所有用户都能查看字段列表

## 13. 异常处理

### 13.1 上传异常
- 网络失败：提示"网络错误，请稍后重试"
- 文件过大：提示"文件大小超过2MB限制"
- 格式错误：提示具体的验证错误信息
- 权限不足：提示"您没有上传权限"
- 版本冲突：提示"版本号已存在，请重试"

### 13.2 版本切换异常
- 版本不存在：提示"目标版本不存在"
- 权限不足：提示"您没有切换版本的权限"
- 数据库失败：提示"版本切换失败，请稍后重试"

### 13.3 版本对比异常
- 版本不存在：提示"对比版本不存在"
- 数据格式异常：提示"版本数据异常，无法对比"
- 对比失败：提示"版本对比失败，请稍后重试"

### 13.4 数据加载异常
- 接口失败：显示错误提示，使用兜底数据
- 数据为空：显示标准字段，提示"未上传配置"
- 数据格式异常：记录日志，使用兜底数据

### 13.5 文件存储异常
- 文件保存失败：回滚数据库事务
- 磁盘空间不足：提示管理员
- 路径权限问题：记录日志，提示系统错误

## 14. 性能要求
- 上传文件处理时间 < 3秒（2MB以内）
- 页面加载时间 < 1秒
- 上传历史记录查询 < 500ms
- 版本对比计算 < 1秒
- 支持至少1000个字段的JSON文件
- 支持至少100个历史版本

## 15. UI/UX设计要点

### 15.1 色彩规范
- 当前版本：主题色（蓝色）标识
- 新增字段：绿色（#52c41a）
- 删除字段：红色（#ff4d4f）
- 修改字段：橙色（#faad14）
- 未变更字段：灰色（#8c8c8c）

### 15.2 交互反馈
- 上传成功：显示成功提示，自动关闭弹窗
- 版本切换：显示确认对话框，切换后显示成功提示
- 版本对比：加载时显示骨架屏，对比完成后展示结果
- 文件选择：显示文件预览信息

### 15.3 响应式设计
- 表格在小屏幕上支持横向滚动
- 弹窗在小屏幕上占满屏幕
- 版本对比在小屏幕上垂直布局（上下对比）

## 16. 后续优化建议
- 支持Excel文件导入（自动转换为JSON）
- 支持在线编辑JSON（可视化表单）
- 支持版本回滚（撤销切换）
- 支持版本标签（给重要版本打标签）
- 支持批量导入多个甲方配置
- 支持字段配置模板库（预设常用配置）
- 支持版本对比结果导出为PDF
- 支持版本变更通知（邮件/站内信）
- 支持版本审批流程（大版本需审批）
- 支持A/B测试（多版本并行）

## 17. 与详情场景的区分

### 17.1 场景标识
| 项目 | 案件列表 | 案件详情 |
|------|---------|---------|
| scene参数 | `list` | `detail` |
| 页面标题 | 案件列表甲方字段查看 | 案件详情甲方字段查看 |
| 路由 | `/field-config/tenant-fields-list` | `/field-config/tenant-fields-detail` |
| 模板文件名 | `tenant_fields_list_template.json` | `tenant_fields_detail_template.json` |
| 是否分组 | 否（平铺） | 是（支持分组） |
| 版本独立性 | 独立版本号 | 独立版本号 |

### 17.2 数据隔离
- 同一甲方的 list 和 detail 场景数据完全隔离
- 版本号独立计算
- 上传历史独立记录
- 当前生效版本独立标识

## 18. 开发检查清单

### 18.1 前端开发
- [ ] 页面标题包含"案件列表"
- [ ] 路由正确配置
- [ ] 模板下载功能
- [ ] 上传弹窗（当前信息+历史记录）
- [ ] 文件上传（拖拽+选择）
- [ ] 上传验证（格式+内容）
- [ ] 版本管理面板
- [ ] 版本详情查看
- [ ] 版本下载功能
- [ ] 版本切换功能
- [ ] 版本对比功能
- [ ] 对比结果展示
- [ ] 对比报告导出
- [ ] 权限控制
- [ ] 错误处理
- [ ] 加载状态

### 18.2 后端开发
- [ ] 数据库表创建
- [ ] 场景参数支持
- [ ] 上传接口
- [ ] 文件验证逻辑
- [ ] 文件存储逻辑
- [ ] 版本管理逻辑
- [ ] 历史记录接口
- [ ] 版本详情接口
- [ ] 版本对比接口
- [ ] 版本切换接口
- [ ] 下载接口
- [ ] 模板接口
- [ ] 权限检查
- [ ] 事务处理
- [ ] 错误日志

### 18.3 测试用例
- [ ] 上传空文件
- [ ] 上传超大文件
- [ ] 上传非JSON文件
- [ ] 上传格式错误的JSON
- [ ] 上传字段缺失的JSON
- [ ] 上传字段重复的JSON
- [ ] 上传Enum缺少枚举值
- [ ] 版本切换
- [ ] 版本对比（新增）
- [ ] 版本对比（删除）
- [ ] 版本对比（修改）
- [ ] 版本对比（无变化）
- [ ] 并发上传
- [ ] 权限测试
- [ ] 性能测试（大文件）
- [ ] 性能测试（大量版本）

---

**文档版本**: 2.0.0
**最后更新**: 2025-12-08
**更新人**: 产品经理
**变更说明**:
- 新增JSON模板下载功能
- 新增上传历史记录展示
- 新增版本管理功能
- 新增版本对比功能（核心）
- 新增版本切换功能
- 明确场景区分（list vs detail）
- 完善验证规则和异常处理
