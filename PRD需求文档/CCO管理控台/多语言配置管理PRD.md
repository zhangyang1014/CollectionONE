# 多语言配置管理 PRD

## 文档信息

| 项目 | 内容 |
|------|------|
| PRD编号 | CCO-CONSOLE-I18N-001 |
| 产品名称 | CollectionONE 多语言配置管理系统 |
| 当前版本 | v1.0 |
| 创建日期 | 2024-12-04 |
| 最后更新 | 2024-12-04 |
| 负责人 | 产品团队 |
| 状态 | 待评审 |
| 优先级 | P0（核心功能） |
| 预期上线 | 第一期：2周内，第二期：4周内 |

## 变更记录

| 版本 | 日期 | 变更内容 | 变更人 |
|------|------|----------|--------|
| v1.0 | 2024-12-04 | 初始版本 | - |

---

## 一、背景与目标

### 1.1 业务背景

CollectionONE 正在进行全球化扩张，需要支持多国语言界面以服务不同地区的客户。

**当前痛点：**

1. **语言配置分散**：翻译文案散落在前端代码仓库中，修改需要发布版本
2. **协作困难**：翻译团队无法直接参与，需要通过开发人员中转
3. **缺乏管理**：无法统计翻译进度、缺失的翻译项
4. **运营受限**：无法快速响应市场需求，新增语言周期长
5. **质量难控**：缺少翻译审核机制，质量参差不齐

### 1.2 目标

**业务目标：**
- 支持快速新增语言市场，从需求到上线缩短至 2 个工作日
- 降低翻译成本，提升翻译质量和一致性
- 提升用户体验，支持用户自主切换语言

**产品目标：**
- 在控制台集中管理所有语言配置
- 支持语言包的在线编辑、导入/导出
- 提供翻译进度可视化和质量监控
- 前端实现语言无缝切换，无需刷新页面

### 1.3 非目标（本期不做）

- ❌ 机器翻译能力（AI自动翻译）
- ❌ 实时在线翻译编辑器（所见即所得）
- ❌ 接入第三方翻译平台API（如Crowdin、Phrase）
- ❌ 语音包/多媒体资源的多语言管理
- ❌ 内容管理系统（CMS）的多语言支持

### 1.4 依赖说明

**前置依赖：**
- Vue I18n 框架已集成（前端）
- 用户权限系统已完成
- 租户管理系统已完成

**同步依赖：**
- 需与前端团队协同开发语言切换组件
- 需与运营团队确认初始支持的语言列表

---

## 二、术语与定义

| 术语 | 定义 | 示例 |
|------|------|------|
| **语言（Language）** | 自然语言的名称 | 中文、English、Español |
| **地区（Region）** | 国家或地区代码 | CN、US、MX、ID |
| **Locale** | 语言-地区组合代码（BCP 47标准） | zh-CN、en-US、es-MX、id-ID |
| **语言包（Language Bundle）** | 某个locale对应的所有翻译文案集合 | zh-CN.json |
| **命名空间（Namespace）** | 语言包内的分组，便于模块化管理 | common、auth、dashboard |
| **翻译键（Translation Key）** | 唯一标识一条翻译的路径 | common.button.save |
| **基准语言（Base Language）** | 用于生成翻译模板的语言，通常是产品主要语言 | zh-CN |
| **启用语言** | 前台用户可以选择的语言列表 | - |
| **RTL（Right-to-Left）** | 从右到左的文本方向 | 阿拉伯语、希伯来语 |
| **LTR（Left-to-Right）** | 从左到右的文本方向 | 中文、英语、西班牙语 |

---

## 三、用户角色与权限

### 3.1 角色定义

| 角色 | 说明 | 关键职责 |
|------|------|----------|
| **平台管理员** | 平台运营人员 | 管理所有语言配置、语言包、系统默认语言 |
| **租户管理员** | 各租户的管理员 | 为本租户选择可用语言、设置租户默认语言 |
| **翻译人员** | 专业翻译团队 | 下载语言包、翻译、上传（通过平台管理员） |
| **催员/终端用户** | 前台使用者 | 切换界面语言、使用产品 |

### 3.2 权限矩阵

| 功能 | 平台管理员 | 租户管理员 | 翻译人员 | 终端用户 |
|------|-----------|-----------|---------|---------|
| 新增/删除系统语言 | ✅ | ❌ | ❌ | ❌ |
| 编辑语言元数据 | ✅ | ❌ | ❌ | ❌ |
| 设置系统默认语言 | ✅ | ❌ | ❌ | ❌ |
| 上传/下载语言包 | ✅ | ❌ | 只读下载 | ❌ |
| 查看翻译统计 | ✅ | ✅ | ✅ | ❌ |
| 启用/停用租户可用语言 | ✅ | ✅（仅本租户） | ❌ | ❌ |
| 设置租户默认语言 | ✅ | ✅（仅本租户） | ❌ | ❌ |
| 切换界面语言 | ✅ | ✅ | ✅ | ✅ |

---

## 四、核心功能设计

## 🔧 控制台端功能

### 4.1 系统语言管理

#### 4.1.1 功能入口

**导航路径：**
```
控制台 → 系统设置 → 国际化配置 → 语言管理
```

**权限要求：** 平台管理员

#### 4.1.2 语言列表页

**页面布局：**

```
┌─────────────────────────────────────────────────────────┐
│ 语言管理                                    [+ 新增语言]  │
├─────────────────────────────────────────────────────────┤
│ 🔍 搜索: [___________]  状态: [全部▼]  方向: [全部▼]    │
├─────────────────────────────────────────────────────────┤
│ ☑ │ 国旗 │ 语言名称 │ Locale │ 方向 │ 状态 │ 默认 │ 操作 │
├───┼──────┼─────────┼────────┼──────┼──────┼──────┼──────┤
│ ☑ │ 🇨🇳  │ 中文     │ zh-CN  │ LTR  │ 启用 │ ⭐   │ ···  │
│ ☑ │ 🇺🇸  │ English  │ en-US  │ LTR  │ 启用 │      │ ···  │
│ ☑ │ 🇲🇽  │ Español  │ es-MX  │ LTR  │ 启用 │      │ ···  │
│ ☑ │ 🇮🇩  │ Indonesia│ id-ID  │ LTR  │ 停用 │      │ ···  │
└─────────────────────────────────────────────────────────┘
显示 1-10 条，共 15 条                          [1] 2 3 >
```

**字段说明：**

| 字段 | 说明 | 来源 | 必填 |
|------|------|------|------|
| **国旗图标** | 代表该语言的国旗 | 管理员选择或自动匹配 | 否 |
| **语言名称** | 该语言的自称（本地化名称） | 管理员输入 | ✅ |
| **Locale** | BCP 47 标准代码 | 管理员输入 | ✅ |
| **文本方向** | LTR（左到右）或 RTL（右到左） | 管理员选择 | ✅ |
| **状态** | 启用 / 停用 | 管理员设置 | ✅ |
| **默认语言** | 系统默认语言标识（⭐） | 单选 | - |
| **翻译进度** | 当前翻译完成度（%） | 系统计算 | - |
| **更新时间** | 最后修改时间 | 系统记录 | - |
| **更新人** | 最后操作人 | 系统记录 | - |

**功能操作：**

1. **批量操作**
   - 批量启用/停用
   - 批量导出语言包
   
2. **筛选与搜索**
   - 按语言名称或locale搜索
   - 按状态筛选（全部/启用/停用）
   - 按文本方向筛选（全部/LTR/RTL）
   - 按翻译进度筛选（< 50% / 50-90% / > 90%）

3. **排序**
   - 支持自定义拖拽排序（影响前台语言选择器的顺序）
   - 支持按列排序（名称、locale、更新时间等）

#### 4.1.3 新增/编辑语言

**触发方式：**
- 点击 `[+ 新增语言]` 按钮
- 点击列表中的 `编辑` 操作

**弹窗表单：**

```
┌──────────────────────────────────────┐
│ 新增语言                        [×]   │
├──────────────────────────────────────┤
│ 语言名称（本地化）*                   │
│ [中文____________________________]    │
│ 提示：使用该语言的本地化名称，如"中文"、│
│      "English"、"Español"            │
│                                      │
│ Locale 代码 (BCP 47)*                │
│ [zh-CN__] 🔍 常用locale快速选择       │
│                                      │
│ 国旗图标                              │
│ [选择图标▼] 预览: 🇨🇳                │
│                                      │
│ 文本方向*                             │
│ ⚪ LTR (左到右)  ⚪ RTL (右到左)      │
│                                      │
│ 启用状态                              │
│ [✓] 立即启用此语言                    │
│                                      │
│ 翻译负责人（可选）                     │
│ [选择用户▼]                           │
│                                      │
│ 备注                                  │
│ [_____________________________]      │
│                                      │
├──────────────────────────────────────┤
│              [取消]  [保存]           │
└──────────────────────────────────────┘
```

**校验规则：**

| 字段 | 规则 | 错误提示 |
|------|------|----------|
| 语言名称 | 必填，2-50字符 | "请输入语言名称" |
| Locale | 必填，符合BCP 47格式（如 zh-CN） | "Locale格式不正确，示例：zh-CN" |
| Locale | 不能与已有语言重复 | "该Locale已存在" |
| 国旗图标 | 选填 | - |
| 文本方向 | 必选 | "请选择文本方向" |

**特殊逻辑：**

1. **Locale不可修改规则**
   - 新增时可自由输入
   - 编辑时，若该locale已有翻译数据，则不允许修改
   - 若强制修改，需二次确认：
     ```
     ⚠️  警告
     修改Locale将导致该语言的所有翻译数据失效。
     建议：新增一个语言，然后停用旧语言。
     
     确定要继续吗？
     [取消] [确认修改]
     ```

2. **RTL语言提示**
   - 当选择RTL时，提示：
     ```
     ℹ️  RTL语言会影响整体界面布局方向。
     请确保前端已支持RTL适配，否则可能导致显示错误。
     ```

#### 4.1.4 设置默认语言

**操作方式：**
- 在列表中点击某行的"设为默认"按钮
- 弹出确认框

**确认提示：**
```
设置默认语言

将 [English (en-US)] 设为系统默认语言？

影响范围：
• 未登录用户首次访问时显示此语言
• 用户选择的语言不可用时回退到此语言
• 新注册租户的默认语言

当前默认语言：中文 (zh-CN)

[取消] [确认设置]
```

**约束条件：**
- 只能选择"已启用"的语言作为默认语言
- 系统至少保留一个启用的默认语言（不能停用当前默认语言）

#### 4.1.5 启用/停用语言

**单个停用：**
```
停用语言确认

确定要停用 [Indonesia (id-ID)] 吗？

影响：
• 前台语言选择器中将不再显示此语言
• 已选择此语言的用户将回退到默认语言
• 当前使用此语言的用户数：127 人
• 相关租户：3 个

停用后语言包数据仍保留，可随时重新启用。

[取消] [确认停用]
```

**批量操作：**
- 勾选多个语言 → 点击"批量启用"或"批量停用"
- 提示影响的用户数和租户数

---

### 4.2 语言包管理

#### 4.2.1 功能入口

**导航路径：**
```
控制台 → 系统设置 → 国际化配置 → 语言包管理
```

**Tab结构：**
```
┌─────────────────────────────────────────────┐
│ [语言管理] [语言包管理] [翻译统计]           │
└─────────────────────────────────────────────┘
```

#### 4.2.2 语言包列表

**页面布局：**

```
┌──────────────────────────────────────────────────────────┐
│ 语言包管理                                                │
├──────────────────────────────────────────────────────────┤
│ 基准语言: [中文 (zh-CN) ▼]    [🔄 刷新统计]              │
├──────────────────────────────────────────────────────────┤
│ Locale │ 语言名称 │ 翻译进度 │ 缺失key │ 版本 │ 更新时间 │ 操作    │
├────────┼─────────┼─────────┼────────┼──────┼─────────┼────────┤
│ zh-CN  │ 中文    │ ████ 100%│   0    │ v2.3 │ 2h ago  │ 📥 📤  │
│ en-US  │ English │ ███░  85%│  156   │ v2.1 │ 1d ago  │ 📥 📤 ✏️│
│ es-MX  │ Español │ ██░░  62%│  398   │ v1.8 │ 3d ago  │ 📥 📤 ✏️│
│ id-ID  │Indonesia│ █░░░  35%│  679   │ v1.2 │ 7d ago  │ 📥 📤 ✏️│
└──────────────────────────────────────────────────────────┘

图例: 📥 下载  📤 上传  ✏️ 导出缺失项模板
```

**关键指标：**

| 指标 | 计算方式 | 说明 |
|------|----------|------|
| **翻译进度** | (已翻译key数 / 基准语言总key数) × 100% | 相对于基准语言的完成度 |
| **缺失key数** | 基准语言总key数 - 当前语言已翻译key数 | 还需翻译的条目数 |
| **版本号** | 根据上传时间自动生成或手动指定 | 格式：v{major}.{minor} |

#### 4.2.3 下载语言包

**操作：** 点击 `📥 下载`

**下载选项弹窗：**

```
┌─────────────────────────────────────┐
│ 下载语言包 - English (en-US)   [×]  │
├─────────────────────────────────────┤
│ 下载类型：                           │
│ ⚪ 完整语言包                        │
│    包含所有已翻译的key（1,044条）    │
│                                     │
│ ⚪ 仅缺失项模板                      │
│    包含156个未翻译key，值为基准语言  │
│    便于翻译人员补充                  │
│                                     │
│ 文件格式：                           │
│ ⚪ JSON (标准格式，适合开发)         │
│ ⚪ Excel (适合翻译人员编辑)          │
│                                     │
│ 命名空间（可多选）：                 │
│ ☑ common (通用)                     │
│ ☑ auth (认证)                       │
│ ☑ dashboard (仪表板)                │
│ ☑ case (案件)                       │
│ ☐ payment (还款)                    │
│ ... (展开更多)                       │
│                                     │
├─────────────────────────────────────┤
│            [取消]  [下载]            │
└─────────────────────────────────────┘
```

**下载文件示例：**

**JSON格式（完整语言包）：**
```json
{
  "common": {
    "button": {
      "save": "Save",
      "cancel": "Cancel",
      "delete": "Delete"
    },
    "message": {
      "success": "Operation successful",
      "error": "Operation failed"
    }
  },
  "auth": {
    "login": {
      "title": "Sign In",
      "username": "Username",
      "password": "Password"
    }
  }
}
```

**Excel格式（缺失项模板）：**

| Key | 基准语言(zh-CN) | 目标语言(en-US) | 备注 | 状态 |
|-----|----------------|----------------|------|------|
| common.button.save | 保存 | _(待翻译)_ | 通用按钮 | 缺失 |
| common.button.cancel | 取消 | _(待翻译)_ | 通用按钮 | 缺失 |
| auth.login.title | 登录 | _(待翻译)_ | 登录页标题 | 缺失 |

#### 4.2.4 上传语言包

**操作：** 点击 `📤 上传`

**上传弹窗：**

```
┌──────────────────────────────────────────┐
│ 上传语言包 - English (en-US)        [×]  │
├──────────────────────────────────────────┤
│ 上传文件：                                │
│ ┌────────────────────────────────────┐   │
│ │  拖拽文件到此处或点击上传           │   │
│ │  支持格式：.json, .xlsx            │   │
│ │  最大 5MB                          │   │
│ └────────────────────────────────────┘   │
│                                          │
│ 导入策略：                                │
│ ⚪ 覆盖模式 (推荐)                        │
│    同名key覆盖，新key添加，旧key保留      │
│                                          │
│ ⚪ 仅新增模式                             │
│    只添加不存在的key，已存在的保持不变    │
│                                          │
│ ⚪ 替换模式 (危险)                        │
│    完全替换当前语言包，旧数据清空         │
│                                          │
│ 版本号：                                  │
│ ⚪ 自动递增 (当前 v2.1 → v2.2)           │
│ ⚪ 自定义版本号: [v___]                  │
│                                          │
│ 备注说明：                                │
│ [本次更新了登录页和仪表板的翻译_______]   │
│                                          │
├──────────────────────────────────────────┤
│              [取消]  [开始上传]           │
└──────────────────────────────────────────┘
```

**上传校验：**

上传后显示校验结果：

```
┌──────────────────────────────────────────┐
│ 上传校验结果                              │
├──────────────────────────────────────────┤
│ ✅ 文件格式正确                           │
│ ✅ JSON语法有效                           │
│ ✅ 共检测到 1,200 个key                   │
│                                          │
│ ⚠️  警告 (2项)                            │
│ • 包含 5 个未知key (不在基准语言中)        │
│   - payment.new.feature.title            │
│   - ...                                  │
│                                          │
│ • 覆盖 156 个已存在的key                  │
│                                          │
│ ℹ️  信息                                  │
│ • 新增 44 个key                           │
│ • 总翻译进度：85% → 92%                   │
│                                          │
│ [查看详情]                                │
├──────────────────────────────────────────┤
│        [取消导入]  [确认导入]             │
└──────────────────────────────────────────┘
```

**自动备份：**
- 每次上传前自动备份当前版本
- 备份保留 30 天
- 可在"版本历史"中回滚

#### 4.2.5 版本管理

**入口：** 点击某个语言的版本号

**版本历史列表：**

```
┌──────────────────────────────────────────────────────┐
│ 版本历史 - English (en-US)                      [×]  │
├──────────────────────────────────────────────────────┤
│ 版本   │ 上传时间        │ 操作人   │ 变更说明 │ 操作 │
├────────┼────────────────┼─────────┼─────────┼──────┤
│ v2.1 ✓ │ 2024-12-03 14:30│ 张三     │ 修复登录页│📥 🔄 │
│ v2.0   │ 2024-12-01 10:15│ 李四     │ 大版本更新│📥 🔄 │
│ v1.9   │ 2024-11-28 16:45│ 王五     │ 补充仪表板│📥 🔄 │
│ ...    │                │         │         │      │
└──────────────────────────────────────────────────────┘

✓ = 当前使用版本
图例: 📥 下载此版本  🔄 回滚到此版本
```

**回滚操作：**

点击 `🔄 回滚` 触发确认：

```
⚠️  版本回滚确认

确定要将 English (en-US) 回滚到 v2.0 吗？

当前版本：v2.1 (2024-12-03 14:30)
目标版本：v2.0 (2024-12-01 10:15)

影响：
• 将丢失 v2.1 中的所有变更 (12个key)
• 翻译进度：92% → 90%
• 系统会自动备份当前v2.1版本

[取消] [确认回滚]
```

---

### 4.3 翻译统计与监控

#### 4.3.1 功能入口

**导航路径：**
```
控制台 → 系统设置 → 国际化配置 → 翻译统计
```

#### 4.3.2 总览仪表板

**页面布局：**

```
┌────────────────────────────────────────────────────────┐
│ 翻译统计总览                         刷新时间: 5 min ago│
├────────────────────────────────────────────────────────┤
│ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│ │ 支持语言  │ │ 总翻译key │ │ 平均完成度│ │ 待翻译项  │  │
│ │    15    │ │  1,200   │ │   78%    │ │  1,840   │  │
│ └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
├────────────────────────────────────────────────────────┤
│ 各语言翻译进度                                          │
│                                                        │
│ zh-CN (中文)      ████████████████ 100%  (1,200)      │
│ en-US (English)   █████████████░░░  85%  (1,020)      │
│ es-MX (Español)   ██████████░░░░░░  62%  (  744)      │
│ id-ID (Indonesia) ████░░░░░░░░░░░░  35%  (  420)      │
│ ...                                                    │
│                                                        │
├────────────────────────────────────────────────────────┤
│ 命名空间翻译完成度                                      │
│ ┌────────────────────────────────────────────────┐    │
│ │ common     ████████████████  90% (平均)         │    │
│ │ auth       ████████████████  95%               │    │
│ │ dashboard  ██████████████░░  78%               │    │
│ │ case       ████████████░░░░  68%               │    │
│ │ payment    ██████░░░░░░░░░░  45%               │    │
│ └────────────────────────────────────────────────┘    │
└────────────────────────────────────────────────────────┘
```

#### 4.3.3 缺失翻译详情

**点击任一语言的"缺失key"数字，展开详情：**

```
┌──────────────────────────────────────────────────────────┐
│ 缺失翻译详情 - English (en-US)              [导出Excel]   │
├──────────────────────────────────────────────────────────┤
│ 筛选: 命名空间 [全部▼]  优先级 [全部▼]  🔍 搜索key        │
├──────────────────────────────────────────────────────────┤
│ 翻译Key               │ 基准值(zh-CN) │ 命名空间│ 优先级 │
├───────────────────────┼──────────────┼────────┼────────┤
│ payment.method.alipay │ 支付宝        │ payment│ P1     │
│ payment.method.wechat │ 微信支付      │ payment│ P1     │
│ case.status.overdue   │ 逾期          │ case   │ P0     │
│ dashboard.chart.title │ 数据趋势      │dashboard│ P2    │
│ ...                   │              │        │        │
└──────────────────────────────────────────────────────────┘
显示 1-20 条，共 156 条缺失翻译                  [1] 2 3 >
```

**优先级定义：**
- **P0**：核心功能，影响主流程（如登录、案件列表）
- **P1**：重要功能，影响用户体验
- **P2**：次要功能，不影响使用

**导出Excel：**
- 包含所有缺失key
- 附带基准语言值
- 预留"翻译"列供填写
- 可直接上传回系统

#### 4.3.4 翻译质量检查

**自动检测问题：**

```
┌──────────────────────────────────────────────┐
│ 翻译质量问题                 [🔄 重新检测]    │
├──────────────────────────────────────────────┤
│ ⚠️  发现 23 个潜在问题                        │
│                                              │
│ 类型                │ 数量 │ 查看            │
├────────────────────┼──────┼────────────────┤
│ 缺少变量占位符       │  8   │ [查看详情]     │
│ 长度超出建议         │  5   │ [查看详情]     │
│ 包含HTML标签         │  3   │ [查看详情]     │
│ 复数形式缺失         │  4   │ [查看详情]     │
│ 疑似未翻译(与基准相同)│  3   │ [查看详情]     │
└──────────────────────────────────────────────┘
```

**问题详情示例：**

```
缺少变量占位符 (8项)

Key: common.message.welcome
基准: 欢迎, {username}!
en-US: Welcome!  ❌ (缺少 {username})
建议: Welcome, {username}!

[忽略] [标记已修复]
```

---

### 4.4 租户语言配置

**（仅多租户模式下需要）**

#### 4.4.1 功能入口

**导航路径：**
```
控制台 → 租户管理 → [选择租户] → 语言配置
```

**权限：** 平台管理员 或 该租户管理员

#### 4.4.2 租户可用语言设置

**页面布局：**

```
┌─────────────────────────────────────────────┐
│ 租户语言配置 - ACME Corporation              │
├─────────────────────────────────────────────┤
│ 默认语言: [English (en-US) ▼]               │
│                                             │
│ 可用语言列表:                                │
│ ┌─────────────────────────────────────┐     │
│ │ ☑ zh-CN  中文                       │     │
│ │ ☑ en-US  English   ⭐ (默认)        │     │
│ │ ☑ es-MX  Español                    │     │
│ │ ☐ id-ID  Indonesia  (系统已停用)     │     │
│ │ ☐ ar-SA  العربية   (系统已停用)     │     │
│ └─────────────────────────────────────┘     │
│                                             │
│ 说明:                                        │
│ • 只能选择系统已启用的语言                   │
│ • 默认语言必须在可用语言列表中               │
│ • 租户用户只能切换到此列表中的语言           │
│                                             │
├─────────────────────────────────────────────┤
│                    [取消]  [保存]            │
└─────────────────────────────────────────────┘
```

**业务规则：**
1. 租户可用语言 ⊆ 系统启用语言
2. 租户默认语言 ∈ 租户可用语言
3. 租户至少保留1个可用语言
4. 修改后对该租户下所有用户立即生效（下次刷新）

---

## 🎨 前端功能设计

### 4.5 语言切换组件

#### 4.5.1 组件位置与样式

**PC端布局：**

```
┌────────────────────────────────────────────────────────┐
│ CollectionONE  [案件] [数据] [设置]    🔔 🇨🇳中文 ▼ 👤  │
└────────────────────────────────────────────────────────┘
                                         ↑
                                    语言切换器
```

**移动端布局：**

```
┌──────────────────────┐
│ ☰                 👤  │  ← 点击头像/菜单
├──────────────────────┤
│  个人中心             │
│  通知                │
│  🇨🇳 语言: 中文 >     │  ← 语言切换入口
│  退出登录             │
└──────────────────────┘
```

#### 4.5.2 下拉菜单交互

**展开状态：**

```
┌────────────────────┐
│ 🇨🇳 中文         ✓  │  ← 当前选中
├────────────────────┤
│ 🇺🇸 English         │
│ 🇲🇽 Español         │
│ 🇮🇩 Indonesia       │
└────────────────────┘
```

**交互细节：**

| 操作 | 行为 |
|------|------|
| **点击入口** | 展开语言列表 |
| **鼠标悬停** | 高亮该语言行 |
| **点击语言** | 立即切换，下拉收起，页面文案更新 |
| **点击外部** | 下拉收起 |
| **按ESC键** | 下拉收起 |

**键盘支持：**
- **Tab**：聚焦语言切换器
- **Enter/Space**：展开下拉
- **↑/↓**：在列表中上下移动
- **Enter**：选中当前高亮语言
- **ESC**：关闭下拉

#### 4.5.3 移动端适配

**触发方式：** 点击语言行

**弹窗样式（底部弹出）：**

```
┌────────────────────────┐
│                        │
│  (背景半透明遮罩)        │
│                        │
│  ┌────────────────────┐│
│  │ 选择语言       [×] ││
│  ├────────────────────┤│
│  │ ○ 🇨🇳 中文         ││
│  │ ● 🇺🇸 English ✓    ││
│  │ ○ 🇲🇽 Español      ││
│  │ ○ 🇮🇩 Indonesia    ││
│  └────────────────────┘│
└────────────────────────┘
```

#### 4.5.4 语言切换逻辑

**切换流程：**

```
用户点击语言
    ↓
前端立即切换 i18n locale
    ↓
页面文案即时更新（无刷新）
    ↓
保存到 localStorage
    ↓
(如已登录) 调用后端API保存用户偏好
    ↓
完成
```

**错误处理：**

| 场景 | 处理方式 |
|------|----------|
| **语言包加载失败** | Toast提示"语言切换失败"，回退到之前语言 |
| **后端API保存失败** | 仅localStorage生效，不影响当前使用 |
| **选择的语言已被停用** | 自动回退到系统默认语言，Toast提示 |

#### 4.5.5 语言自动检测与优先级

**语言决策优先级（从高到低）：**

```
1. 用户手动选择的语言 (localStorage: app_lang)
   ↓ (不存在)
2. 后端返回的用户偏好语言 (用户设置)
   ↓ (不存在或不可用)
3. 浏览器语言 (navigator.language)
   ↓ (不在可用列表中)
4. 租户默认语言 (如有租户概念)
   ↓ (不存在)
5. 系统默认语言 (最终兜底)
```

**浏览器语言匹配规则：**

```javascript
// 示例逻辑
const browserLang = navigator.language // 如 "zh-CN", "zh", "en-US"
const availableLocales = ["zh-CN", "en-US", "es-MX", "id-ID"]

// 完全匹配
if (availableLocales.includes(browserLang)) {
  return browserLang // "zh-CN"
}

// 语言代码匹配 (忽略地区)
const langCode = browserLang.split('-')[0] // "zh"
const matched = availableLocales.find(loc => loc.startsWith(langCode))
if (matched) {
  return matched // "zh-CN"
}

// 都不匹配，使用系统默认
return systemDefaultLocale
```

---

### 4.6 国际化最佳实践

#### 4.6.1 日期时间格式化

**统一使用 i18n 提供的日期格式化方法**

```javascript
// ❌ 错误：硬编码格式
const date = '2024-12-04 14:30'

// ✅ 正确：使用 i18n
import { useI18n } from 'vue-i18n'
const { d } = useI18n()

// 短日期: 2024/12/04
d(new Date(), 'short')

// 长日期: 2024年12月4日 14:30
d(new Date(), 'long')
```

**配置示例：**

```json
{
  "datetimeFormats": {
    "zh-CN": {
      "short": {
        "year": "numeric",
        "month": "2-digit",
        "day": "2-digit"
      },
      "long": {
        "year": "numeric",
        "month": "long",
        "day": "numeric",
        "hour": "2-digit",
        "minute": "2-digit"
      }
    },
    "en-US": {
      "short": {
        "year": "numeric",
        "month": "short",
        "day": "numeric"
      },
      "long": {
        "year": "numeric",
        "month": "long",
        "day": "numeric",
        "hour": "2-digit",
        "minute": "2-digit",
        "hour12": true
      }
    }
  }
}
```

#### 4.6.2 数字与货币格式化

```javascript
import { useI18n } from 'vue-i18n'
const { n } = useI18n()

// 千分位分隔
n(1234567.89, 'decimal')
// zh-CN: 1,234,567.89
// de-DE: 1.234.567,89

// 货币
n(1234.56, 'currency')
// zh-CN: ¥1,234.56
// en-US: $1,234.56
// id-ID: Rp1.234,56
```

**配置示例：**

```json
{
  "numberFormats": {
    "zh-CN": {
      "currency": {
        "style": "currency",
        "currency": "CNY"
      },
      "decimal": {
        "style": "decimal",
        "minimumFractionDigits": 2,
        "maximumFractionDigits": 2
      }
    },
    "en-US": {
      "currency": {
        "style": "currency",
        "currency": "USD"
      }
    }
  }
}
```

#### 4.6.3 复数形式处理

**不同语言的复数规则不同：**

```json
{
  "zh-CN": {
    "case": {
      "count": "{count} 个案件"
    }
  },
  "en-US": {
    "case": {
      "count": "no cases | one case | {count} cases"
    }
  }
}
```

**使用方式：**

```vue
<template>
  {{ $t('case.count', { count: caseCount }, caseCount) }}
</template>
```

**输出：**
- `caseCount = 0`: "no cases"
- `caseCount = 1`: "one case"
- `caseCount = 5`: "5 cases"

#### 4.6.4 变量插值规范

**✅ 推荐：使用命名参数**

```json
{
  "welcome": "欢迎, {username}! 您有 {count} 个待处理案件"
}
```

```javascript
t('welcome', { username: '张三', count: 5 })
// 输出: 欢迎, 张三! 您有 5 个待处理案件
```

**❌ 不推荐：位置参数**

```json
{
  "welcome": "欢迎, {0}! 您有 {1} 个待处理案件"
}
```

*原因：不同语言的语序可能不同，命名参数更灵活*

#### 4.6.5 RTL语言支持

**CSS适配：**

```css
/* 使用逻辑属性 */
.container {
  /* ❌ 不要用固定方向 */
  /* margin-left: 16px; */
  
  /* ✅ 使用逻辑属性 */
  margin-inline-start: 16px;
  padding-inline: 12px;
}

/* 根据语言方向调整 */
[dir="rtl"] .icon-arrow {
  transform: scaleX(-1); /* 镜像翻转 */
}
```

**HTML设置：**

```vue
<template>
  <html :dir="direction" :lang="locale">
    <!-- direction = 'ltr' 或 'rtl' -->
  </html>
</template>

<script setup>
import { computed } from 'vue'
import { useI18n } from 'vue-i18n'

const { locale } = useI18n()
const direction = computed(() => {
  const rtlLocales = ['ar-SA', 'he-IL', 'fa-IR']
  return rtlLocales.includes(locale.value) ? 'rtl' : 'ltr'
})
</script>
```

---

## 五、数据模型

### 5.1 语言表 (languages)

| 字段 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| id | bigint | ✅ | 主键 | 1 |
| locale | varchar(10) | ✅ | BCP 47标准代码，唯一 | zh-CN |
| name | varchar(100) | ✅ | 语言本地化名称 | 中文 |
| flag_icon | varchar(255) | | 国旗图标URL或代码 | 🇨🇳 或 /icons/cn.svg |
| text_direction | enum | ✅ | ltr 或 rtl | ltr |
| is_enabled | boolean | ✅ | 是否启用 | true |
| is_default | boolean | ✅ | 是否系统默认语言（全局唯一） | false |
| sort_order | int | | 排序值，用于前端显示顺序 | 1 |
| responsible_user_id | bigint | | 翻译负责人ID | 123 |
| created_at | datetime | ✅ | 创建时间 | 2024-12-04 10:00 |
| updated_at | datetime | ✅ | 更新时间 | 2024-12-04 14:30 |
| created_by | bigint | | 创建人ID | 1 |
| updated_by | bigint | | 更新人ID | 2 |
| remarks | text | | 备注 | - |

**索引：**
- UNIQUE KEY `uk_locale` (`locale`)
- KEY `idx_enabled` (`is_enabled`)
- KEY `idx_default` (`is_default`)

**约束：**
- `is_default = true` 的记录全局只能有一条
- `locale` 符合 BCP 47 格式 (如 `^[a-z]{2}-[A-Z]{2}$`)

### 5.2 语言包表 (translation_bundles)

| 字段 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| id | bigint | ✅ | 主键 | 1 |
| language_id | bigint | ✅ | 关联 languages.id | 1 |
| namespace | varchar(50) | ✅ | 命名空间 | common |
| key_path | varchar(255) | ✅ | 翻译键路径 | button.save |
| value | text | ✅ | 翻译值 | 保存 |
| description | varchar(500) | | 说明/备注 | 保存按钮文案 |
| priority | enum | | P0/P1/P2 | P1 |
| status | enum | | draft/published | published |
| version | varchar(20) | | 版本号 | v2.3 |
| created_at | datetime | ✅ | 创建时间 | 2024-12-04 |
| updated_at | datetime | ✅ | 更新时间 | 2024-12-04 |
| updated_by | bigint | | 更新人ID | 2 |

**联合唯一键：**
- UNIQUE KEY `uk_lang_ns_key` (`language_id`, `namespace`, `key_path`)

**索引：**
- KEY `idx_namespace` (`namespace`)
- KEY `idx_language` (`language_id`)
- KEY `idx_version` (`version`)

### 5.3 语言包版本表 (translation_versions)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | bigint | ✅ | 主键 |
| language_id | bigint | ✅ | 关联语言 |
| version | varchar(20) | ✅ | 版本号 (v2.3) |
| bundle_json | mediumtext | ✅ | 完整语言包JSON备份 |
| change_summary | text | | 变更说明 |
| uploaded_by | bigint | | 上传人ID |
| uploaded_at | datetime | ✅ | 上传时间 |
| is_current | boolean | ✅ | 是否当前使用版本 |

**索引：**
- UNIQUE KEY `uk_lang_version` (`language_id`, `version`)
- KEY `idx_current` (`is_current`)

### 5.4 租户语言配置表 (tenant_language_configs)

*(仅多租户模式)*

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | bigint | ✅ | 主键 |
| tenant_id | bigint | ✅ | 租户ID |
| language_id | bigint | ✅ | 语言ID |
| is_enabled | boolean | ✅ | 该租户是否启用此语言 |
| is_default | boolean | ✅ | 是否该租户默认语言 |
| created_at | datetime | ✅ | 创建时间 |
| updated_at | datetime | ✅ | 更新时间 |

**约束：**
- UNIQUE KEY `uk_tenant_lang` (`tenant_id`, `language_id`)
- 同一 `tenant_id` 下只能有一条 `is_default = true`

### 5.5 用户语言偏好表 (user_language_preferences)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | bigint | ✅ | 主键 |
| user_id | bigint | ✅ | 用户ID，唯一 |
| language_id | bigint | ✅ | 偏好语言ID |
| updated_at | datetime | ✅ | 更新时间 |

**索引：**
- UNIQUE KEY `uk_user` (`user_id`)

---

## 六、接口设计（概要）

### 6.1 控制台API

| 接口 | 方法 | 说明 | 权限 |
|------|------|------|------|
| `/admin/languages` | GET | 获取语言列表 | 平台管理员 |
| `/admin/languages` | POST | 新增语言 | 平台管理员 |
| `/admin/languages/{id}` | PUT | 编辑语言 | 平台管理员 |
| `/admin/languages/{id}/enable` | PUT | 启用语言 | 平台管理员 |
| `/admin/languages/{id}/disable` | PUT | 停用语言 | 平台管理员 |
| `/admin/languages/default` | PUT | 设置默认语言 | 平台管理员 |
| `/admin/languages/{locale}/bundle` | GET | 下载语言包 | 平台管理员 |
| `/admin/languages/{locale}/bundle` | POST | 上传语言包 | 平台管理员 |
| `/admin/languages/{locale}/versions` | GET | 查看版本历史 | 平台管理员 |
| `/admin/languages/{locale}/versions/{ver}/rollback` | POST | 回滚版本 | 平台管理员 |
| `/admin/translations/statistics` | GET | 获取翻译统计 | 平台管理员 |
| `/admin/translations/missing` | GET | 获取缺失翻译列表 | 平台管理员 |
| `/admin/tenants/{id}/languages` | GET | 租户语言配置 | 租户管理员 |
| `/admin/tenants/{id}/languages` | PUT | 更新租户语言配置 | 租户管理员 |

### 6.2 前端API

| 接口 | 方法 | 说明 | 权限 |
|------|------|------|------|
| `/i18n/languages` | GET | 获取可用语言列表 | 公开 |
| `/i18n/locales/{locale}` | GET | 获取某语言的翻译包（JSON） | 公开 |
| `/user/language` | GET | 获取用户语言偏好 | 登录用户 |
| `/user/language` | PUT | 保存用户语言偏好 | 登录用户 |

**示例响应（GET `/i18n/languages`）：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "defaultLocale": "zh-CN",
    "languages": [
      {
        "id": 1,
        "locale": "zh-CN",
        "name": "中文",
        "flagIcon": "🇨🇳",
        "textDirection": "ltr",
        "sortOrder": 1
      },
      {
        "id": 2,
        "locale": "en-US",
        "name": "English",
        "flagIcon": "🇺🇸",
        "textDirection": "ltr",
        "sortOrder": 2
      }
    ]
  }
}
```

**示例响应（GET `/i18n/locales/zh-CN`）：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "locale": "zh-CN",
    "version": "v2.3",
    "translations": {
      "common": {
        "button": {
          "save": "保存",
          "cancel": "取消"
        }
      },
      "auth": {
        "login": {
          "title": "登录"
        }
      }
    }
  }
}
```

---

## 七、用户故事与验收标准

### 7.1 核心用户故事

#### 故事1：平台管理员新增语言

**作为** 平台管理员  
**我想要** 在控制台新增一个语言（如印尼语）  
**以便** 扩展到新市场，支持印尼用户

**验收标准：**
- [ ] 能够填写语言名称、locale、选择国旗、文本方向
- [ ] 保存后，该语言出现在语言列表中
- [ ] 默认状态为"启用"
- [ ] 翻译进度显示为 0%
- [ ] 前端刷新后，语言选择器中可见该语言（如已启用）

#### 故事2：翻译人员下载缺失翻译模板

**作为** 翻译人员  
**我想要** 下载英语的缺失翻译Excel模板  
**以便** 补充未翻译的内容

**验收标准：**
- [ ] 能够选择"仅缺失项模板"和"Excel格式"
- [ ] 下载的文件包含所有缺失key
- [ ] 每行包含：翻译key、基准语言值、空白翻译列
- [ ] Excel文件可正常打开编辑
- [ ] 填写后可上传回系统

#### 故事3：前端用户切换语言

**作为** 催员用户  
**我想要** 点击右上角切换到English  
**以便** 使用我熟悉的语言操作系统

**验收标准：**
- [ ] 点击语言切换器，展开可用语言列表
- [ ] 点击English，页面所有文案立即切换（无刷新）
- [ ] 当前语言显示为English，下拉中有选中标识
- [ ] 下次登录仍显示English
- [ ] 切换到其他设备登录，也显示English

#### 故事4：租户管理员设置可用语言

**作为** 租户管理员  
**我想要** 为我的租户只启用中文和英语  
**以便** 简化用户选择，避免误操作

**验收标准：**
- [ ] 能够勾选中文和英语，取消勾选其他语言
- [ ] 保存后，该租户用户只能看到中文和英语
- [ ] 其他租户不受影响
- [ ] 可以设置租户默认语言为英语

#### 故事5：平台管理员上传语言包

**作为** 平台管理员  
**我想要** 上传翻译团队提供的英语语言包JSON  
**以便** 快速更新翻译内容

**验收标准：**
- [ ] 能够选择文件并上传
- [ ] 上传前显示校验结果（新增、覆盖、未知key）
- [ ] 确认后，翻译进度更新
- [ ] 前端刷新后，新翻译生效
- [ ] 可以在版本历史中查看到新版本
- [ ] 旧版本自动备份，可回滚

---

### 7.2 边界场景与异常处理

| 场景 | 预期行为 |
|------|----------|
| **停用当前默认语言** | 提示错误："默认语言不能停用，请先设置其他语言为默认" |
| **删除已有翻译数据的locale** | 二次确认，提示会丢失所有翻译数据 |
| **上传的JSON格式错误** | 显示错误："JSON格式无效，请检查文件" |
| **上传包含未知key** | 警告提示，但允许导入（打上"未知"标签） |
| **用户选择的语言被停用** | 自动回退到系统默认语言，Toast提示 |
| **浏览器不支持的语言** | 匹配语言代码（zh → zh-CN），无匹配则用默认 |
| **语言包加载失败（网络错误）** | 显示Toast："语言切换失败，请重试"，保持当前语言 |
| **并发上传同一语言包** | 后端乐观锁，提示"版本冲突，请刷新后重试" |
| **RTL语言切换** | 整体布局方向切换（dir="rtl"），图标镜像 |
| **缺失某些命名空间的翻译** | 回退到基准语言或显示key |

---

## 八、非功能需求

### 8.1 性能要求

| 指标 | 要求 | 说明 |
|------|------|------|
| **语言列表加载** | < 500ms | 首次加载可用语言列表 |
| **语言包加载** | < 1s | 单个语言包JSON加载时间 |
| **语言切换响应** | < 200ms | 点击语言到界面更新的时间 |
| **语言包大小** | < 200KB | 单个语言包JSON文件大小 |
| **并发上传** | 支持 10 并发 | 多管理员同时操作 |

**优化策略：**
- 语言包按命名空间拆分，按需加载
- 使用CDN加速静态语言包文件
- 前端缓存语言列表（session级别）
- 后端缓存热门语言包（Redis）

### 8.2 安全要求

| 要求 | 实现方式 |
|------|----------|
| **权限校验** | 所有管理端API需验证平台管理员权限 |
| **输入校验** | locale格式、JSON语法、文件大小限制 |
| **XSS防护** | 翻译值输出时HTML转义 |
| **SQL注入防护** | 使用参数化查询 |
| **文件上传安全** | 限制文件类型(.json, .xlsx)、大小(5MB) |
| **审计日志** | 记录所有语言配置变更操作 |

### 8.3 可访问性要求

| 要求 | 实现方式 |
|------|----------|
| **键盘导航** | Tab、Enter、Esc、方向键支持 |
| **ARIA属性** | aria-haspopup, aria-expanded, aria-label |
| **屏幕阅读器** | 语言切换器有明确的语义标签 |
| **颜色对比度** | 符合WCAG AA标准（4.5:1） |

### 8.4 兼容性要求

**浏览器：**
- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

**移动端：**
- iOS Safari 14+
- Android Chrome 90+

**语言包格式：**
- Vue I18n 9.x 兼容

---

## 九、技术约束与建议

### 9.1 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue.js | 3.x | 前端框架 |
| Vue I18n | 9.x | 国际化核心库 |
| Pinia | 2.x | 状态管理（存储当前语言） |
| Axios | 1.x | API请求 |

### 9.2 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 3.3.5 | 后端框架 |
| MyBatis-Plus | 3.5.8 | ORM |
| MySQL | 8.0+ | 数据库 |
| Redis | 6.x | 缓存语言包 |

### 9.3 语言包组织建议

**目录结构：**

```
frontend/
  src/
    locales/
      zh-CN/
        common.json      # 通用文案
        auth.json        # 认证模块
        dashboard.json   # 仪表板
        case.json        # 案件管理
        ...
      en-US/
        common.json
        auth.json
        ...
      index.ts           # 语言包导出
```

**懒加载示例：**

```typescript
// locales/index.ts
export const loadLocaleMessages = async (locale: string) => {
  const messages = await import(
    /* webpackChunkName: "locale-[request]" */
    `./${locale}/index.json`
  )
  return messages.default
}
```

### 9.4 命名规范

**翻译key命名规则：**
- 使用小写字母和点号分隔：`module.component.element`
- 避免过长路径（< 5 层）
- 使用语义化名称，避免缩写

**示例：**
```
✅ common.button.save
✅ auth.login.username_placeholder
✅ dashboard.chart.revenue_title

❌ btn_save
❌ auth.login.page.form.input.username.placeholder.text (过长)
```

---

## 十、数据埋点

### 10.1 关键事件

| 事件名 | 触发时机 | 属性 |
|--------|----------|------|
| `language_changed` | 用户切换语言 | from_locale, to_locale, user_id, is_login, trigger_source(manual/auto) |
| `language_auto_detected` | 自动检测浏览器语言 | detected_locale, final_locale, match_type(exact/partial/fallback) |
| `translation_bundle_loaded` | 语言包加载成功 | locale, load_time_ms, bundle_size_kb |
| `translation_bundle_failed` | 语言包加载失败 | locale, error_message |
| `admin_language_created` | 管理员新增语言 | locale, name, created_by |
| `admin_language_enabled` | 管理员启用语言 | locale, enabled_by |
| `admin_language_disabled` | 管理员停用语言 | locale, affected_users_count, disabled_by |
| `admin_bundle_uploaded` | 管理员上传语言包 | locale, version, new_keys_count, updated_keys_count |
| `admin_bundle_rollback` | 管理员回滚版本 | locale, from_version, to_version |

### 10.2 业务指标

| 指标 | 说明 | 用途 |
|------|------|------|
| **各语言使用占比** | 每个语言的活跃用户数占比 | 评估市场重要性 |
| **语言切换频率** | 每日语言切换次数 | 评估多语言用户比例 |
| **翻译完成度** | 各语言的翻译进度 | 监控翻译工作进展 |
| **缺失翻译展示次数** | 前端fallback到key的次数 | 发现翻译质量问题 |
| **语言切换后留存率** | 切换语言后的次日留存 | 评估本地化效果 |

---

## 十一、迭代规划

### 11.1 第一期（MVP）- 2周

**目标：** 核心功能上线，支持基础多语言切换

**功能范围：**
- ✅ 控制台语言管理（新增、编辑、启用/停用）
- ✅ 设置系统默认语言
- ✅ 前端语言切换组件（PC端）
- ✅ 本地JSON语言包（zh-CN, en-US）
- ✅ 基础翻译统计

**技术实现：**
- 数据库表结构设计
- 控制台管理页面开发
- 前端LanguageSwitcher组件开发
- Vue I18n集成
- 基础API接口

**验收标准：**
- 能够新增/编辑/启用/停用语言
- 前端可切换中文/英语，文案即时更新
- 用户偏好保存到localStorage和后端

### 11.2 第二期 - +2周

**目标：** 完善语言包管理，支持翻译协作

**功能范围：**
- ✅ 语言包上传/下载（JSON & Excel）
- ✅ 缺失翻译详情与导出
- ✅ 版本管理与回滚
- ✅ 翻译质量检查
- ✅ 移动端语言切换适配

**技术实现：**
- 文件上传/下载功能
- Excel解析与生成
- 版本备份机制
- 翻译质量检测规则

**验收标准：**
- 能够上传/下载语言包
- 能够导出缺失翻译模板
- 能够查看版本历史并回滚
- 移动端语言切换体验良好

### 11.3 第三期（可选）- +3周

**目标：** 高级功能，提升效率

**功能范围：**
- ⭐ 租户级语言配置
- ⭐ 在线翻译编辑器（简易版）
- ⭐ 翻译审核流程
- ⭐ 接入翻译平台API（如Crowdin）
- ⭐ RTL语言完整适配
- ⭐ 多命名空间按需加载优化

**技术实现：**
- 租户多语言隔离
- 在线编辑器组件
- 审核工作流
- 第三方API对接

**验收标准：**
- 租户管理员可独立配置语言
- 可在线编辑部分翻译
- 翻译变更需审核后生效
- RTL语言布局正确

---

## 十二、测试用例概要

### 12.1 功能测试

| 模块 | 测试点 | 预期结果 |
|------|--------|----------|
| **语言管理** | 新增语言 | 保存成功，列表中出现 |
| | 编辑语言名称 | 更新成功 |
| | 修改已有数据的locale | 提示警告，需二次确认 |
| | 启用/停用语言 | 状态正确变更，前端同步 |
| | 设置默认语言 | 标识正确，只能有一个默认 |
| | 停用默认语言 | 提示错误，禁止操作 |
| **语言包管理** | 下载完整语言包 | JSON格式正确 |
| | 下载缺失翻译模板 | 包含所有缺失key |
| | 上传合法JSON | 导入成功，进度更新 |
| | 上传格式错误的JSON | 提示错误，禁止导入 |
| | 上传包含未知key | 警告但允许导入 |
| | 版本回滚 | 恢复到指定版本 |
| **前端切换** | 点击语言切换器 | 展开语言列表 |
| | 选择语言 | 文案即时更新 |
| | 刷新页面 | 保持选择的语言 |
| | 切换设备登录 | 语言偏好同步 |
| **翻译统计** | 查看翻译进度 | 百分比正确 |
| | 查看缺失列表 | key准确 |
| | 导出缺失模板 | Excel正确 |

### 12.2 性能测试

| 场景 | 指标 | 目标 |
|------|------|------|
| 语言列表加载 | 响应时间 | < 500ms |
| 语言包下载 | 文件大小 | < 200KB |
| 语言切换 | 界面更新时间 | < 200ms |
| 并发上传 | 10并发 | 无错误 |

### 12.3 兼容性测试

| 浏览器 | 版本 | 测试点 |
|--------|------|--------|
| Chrome | 90+ | 所有功能正常 |
| Firefox | 88+ | 所有功能正常 |
| Safari | 14+ | 所有功能正常 |
| Mobile Safari | iOS 14+ | 移动端切换正常 |
| Android Chrome | 90+ | 移动端切换正常 |

### 12.4 安全测试

| 测试点 | 攻击方式 | 预期防护 |
|--------|----------|----------|
| XSS | 翻译值插入`<script>` | HTML转义，无执行 |
| SQL注入 | locale输入`' OR '1'='1` | 参数化查询，无效 |
| 文件上传 | 上传.exe文件 | 限制格式，拒绝 |
| 权限绕过 | 非管理员调用管理API | 401/403错误 |

---

## 十三、风险与应对

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| **翻译质量差** | 用户体验下降 | 中 | 1. 翻译质量检查 2. 审核流程 3. 用户反馈机制 |
| **语言包过大** | 加载慢，首屏性能差 | 中 | 1. 按命名空间拆分 2. 懒加载 3. CDN加速 |
| **RTL适配复杂** | 布局错乱 | 高 | 1. 第一期不支持RTL 2. 第三期专项优化 |
| **并发上传冲突** | 数据覆盖 | 低 | 1. 乐观锁 2. 版本校验 3. 冲突提示 |
| **浏览器语言检测不准** | 用户看到错误语言 | 中 | 1. 完善匹配规则 2. 明显的切换入口 |
| **翻译协作流程复杂** | 效率低 | 中 | 1. 提供Excel模板 2. 清晰的操作文档 |

---

## 十四、文档与培训

### 14.1 需交付文档

| 文档 | 受众 | 内容 |
|------|------|------|
| **技术设计文档** | 开发团队 | 详细的技术实现方案、数据库设计、API文档 |
| **管理员操作手册** | 平台管理员 | 语言管理、语言包上传/下载、翻译统计使用说明 |
| **翻译协作指南** | 翻译团队 | 如何下载模板、填写翻译、上传流程 |
| **前端集成文档** | 前端开发 | Vue I18n使用规范、组件调用方式、命名规范 |
| **用户使用说明** | 终端用户 | 如何切换语言（图文教程） |

### 14.2 培训计划

**对象：平台管理员**
- 时长：1小时
- 内容：语言管理、语言包上传、翻译统计查看

**对象：翻译团队**
- 时长：30分钟
- 内容：下载模板、翻译规范、质量标准

---

## 十五、附录

### 15.1 常见BCP 47 Locale代码

| Locale | 语言名称 | 地区 |
|--------|----------|------|
| zh-CN | 中文（简体） | 中国大陆 |
| zh-TW | 中文（繁体） | 台湾 |
| en-US | English | 美国 |
| en-GB | English | 英国 |
| es-MX | Español | 墨西哥 |
| es-ES | Español | 西班牙 |
| id-ID | Indonesia | 印度尼西亚 |
| vi-VN | Tiếng Việt | 越南 |
| th-TH | ไทย | 泰国 |
| ja-JP | 日本語 | 日本 |
| ko-KR | 한국어 | 韩国 |
| ar-SA | العربية | 沙特阿拉伯 |
| fr-FR | Français | 法国 |
| de-DE | Deutsch | 德国 |
| pt-BR | Português | 巴西 |
| ru-RU | Русский | 俄罗斯 |

### 15.2 翻译质量检查规则

| 规则 | 说明 | 示例 |
|------|------|------|
| **变量占位符一致** | 翻译值需包含所有变量 | 基准：`欢迎, {username}` → 翻译：`Welcome, {username}` |
| **长度合理性** | 翻译长度不超过基准的2倍 | 基准：10字符 → 翻译：<20字符 |
| **HTML标签闭合** | 包含HTML时标签需闭合 | `<b>Bold</b>` ✅, `<b>Bold` ❌ |
| **复数形式完整** | 英语需包含 `| |` 分隔 | `no items | one item | {n} items` |
| **禁止相同值** | 翻译值不应与基准语言完全相同（可能未翻译） | 基准：`保存` → 翻译：`保存` ❌ |
| **特殊字符转义** | 引号、换行符等需正确转义 | `He said \"Hi\"` |

### 15.3 参考资源

- [BCP 47 Language Tags](https://www.rfc-editor.org/info/bcp47)
- [Vue I18n Documentation](https://vue-i18n.intlify.dev/)
- [CLDR - Unicode Common Locale Data Repository](https://cldr.unicode.org/)
- [WCAG 2.1 Guidelines](https://www.w3.org/WAI/WCAG21/quickref/)
- [RTL Styling Best Practices](https://rtlstyling.com/)

---

## 十六、批准签字

| 角色 | 姓名 | 签字 | 日期 |
|------|------|------|------|
| 产品负责人 | | | |
| 技术负责人 | | | |
| 运营负责人 | | | |

---

**文档结束**

*本PRD为CollectionONE多语言配置管理系统的产品需求文档，后续如有变更需通过正式流程更新版本号。*

<function_calls>
<invoke name="read_file">
<parameter name="target_file">/Users/zhangyang/Documents/GitHub/CollectionONE/PRD需求文档/CCO管理控台/案件分配功能PRD.md
