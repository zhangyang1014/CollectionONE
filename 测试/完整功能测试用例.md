# CCO 催收系统 - 完整功能测试用例

## 📋 测试概述

本测试用例覆盖 CCO 催收系统的全部功能，从基础数据创建到业务流程完整验证。

**测试环境**:
- 后端: `http://localhost:8000`
- 前端: `http://localhost:5173`
- 数据库: SQLite/MySQL

---

## 🔐 一、认证授权测试

### 1.1 SuperAdmin 登录测试

**测试目的**: 验证超级管理员登录功能

**前置条件**: 无

**测试步骤**:
```bash
curl -X POST http://localhost:8000/api/v1/admin/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "loginId": "superadmin",
    "password": "123456"
  }'
```

**预期结果**:
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 1,
      "loginId": "superadmin",
      "username": "superadmin",
      "role": "SuperAdmin",
      "email": "admin@cco.com",
      "name": "超级管理员"
    }
  }
}
```

**验证点**:
- ✅ 返回 code 为 200
- ✅ 返回有效的 JWT token
- ✅ 返回用户信息包含 role = "SuperAdmin"

### 1.2 TenantAdmin 登录测试

**测试步骤**:
```bash
curl -X POST http://localhost:8000/api/v1/admin/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "loginId": "tenantadmin",
    "password": "admin123"
  }'
```

**预期结果**: 返回甲方管理员的 token 和信息

### 1.3 IM端催员登录测试

**测试步骤**:
```bash
curl -X POST http://localhost:8000/api/v1/im/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "loginId": "collector001",
    "password": "123456"
  }'
```

**预期结果**: 返回催员的 token 和信息

### 1.4 Token 验证测试

**测试步骤**:
```bash
curl http://localhost:8000/api/v1/admin/auth/me \
  -H "Authorization: Bearer {token}"
```

**预期结果**: 返回当前登录用户信息

---

## 🏢 二、基础数据创建测试

### 2.1 创建甲方（Tenant）

**测试目的**: 创建甲方A、甲方B

**前置条件**: SuperAdmin 已登录

**测试步骤 - 创建甲方A**:
```bash
curl -X POST http://localhost:8000/api/v1/tenants \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_code": "TENANT_A",
    "tenant_name": "甲方A公司",
    "tenant_name_en": "Tenant A Corp",
    "country_code": "PH",
    "timezone": 8,
    "currency_code": "PHP",
    "is_active": true
  }'
```

**预期结果**:
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 1,
    "tenant_code": "TENANT_A",
    "tenant_name": "甲方A公司",
    "is_active": true,
    "created_at": "2025-11-20 21:00:00"
  }
}
```

**测试步骤 - 创建甲方B**:
```bash
curl -X POST http://localhost:8000/api/v1/tenants \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_code": "TENANT_B",
    "tenant_name": "甲方B公司",
    "tenant_name_en": "Tenant B Corp",
    "country_code": "ID",
    "timezone": 7,
    "currency_code": "IDR",
    "is_active": true
  }'
```

**验证点**:
- ✅ 返回新创建的甲方 ID
- ✅ tenant_code 唯一
- ✅ 甲方状态为 active

### 2.2 创建催收机构（Agency）

**测试目的**: 为甲方A创建2个催收机构

**前置条件**: 甲方A已创建（tenant_id = 1）

**测试步骤 - 创建机构1**:
```bash
curl -X POST http://localhost:8000/api/v1/agencies \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_id": 1,
    "agency_code": "AGENCY_001",
    "agency_name": "催收机构1",
    "agency_name_en": "Collection Agency 1",
    "contact_person": "张三",
    "contact_phone": "+63-912-345-6789",
    "contact_email": "agency1@example.com",
    "address": "马尼拉市中心大楼",
    "timezone": 8,
    "agency_type": "real",
    "is_active": true
  }'
```

**预期结果**: 返回机构ID = 1

**测试步骤 - 创建机构2**:
```bash
curl -X POST http://localhost:8000/api/v1/agencies \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_id": 1,
    "agency_code": "AGENCY_002",
    "agency_name": "催收机构2",
    "agency_name_en": "Collection Agency 2",
    "contact_person": "李四",
    "contact_phone": "+63-917-888-9999",
    "contact_email": "agency2@example.com",
    "address": "宿务市分部",
    "timezone": 8,
    "agency_type": "real",
    "is_active": true
  }'
```

**预期结果**: 返回机构ID = 2

**验证点**:
- ✅ 机构成功关联到甲方A
- ✅ agency_code 在同一甲方内唯一
- ✅ 机构状态为 active

### 2.3 创建案件队列（Queue）

**测试目的**: 为甲方A创建标准队列（M1, M2, M3+, M4+, LEGAL）

**前置条件**: 甲方A已创建

**测试步骤 - 创建M1队列**:
```bash
curl -X POST http://localhost:8000/api/v1/queues \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_id": 1,
    "queue_code": "M1",
    "queue_name": "M1队列（1-30天）",
    "queue_name_en": "M1 Queue (1-30 days)",
    "queue_description": "逾期1-30天的案件",
    "overdue_days_start": 1,
    "overdue_days_end": 30,
    "sort_order": 1,
    "is_active": true
  }'
```

**测试步骤 - 创建M2队列**:
```bash
curl -X POST http://localhost:8000/api/v1/queues \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_id": 1,
    "queue_code": "M2",
    "queue_name": "M2队列（31-60天）",
    "queue_name_en": "M2 Queue (31-60 days)",
    "queue_description": "逾期31-60天的案件",
    "overdue_days_start": 31,
    "overdue_days_end": 60,
    "sort_order": 2,
    "is_active": true
  }'
```

**测试步骤 - 创建M3+队列**:
```bash
curl -X POST http://localhost:8000/api/v1/queues \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_id": 1,
    "queue_code": "M3",
    "queue_name": "M3+队列（61-90天）",
    "queue_name_en": "M3+ Queue (61-90 days)",
    "queue_description": "逾期61-90天的案件",
    "overdue_days_start": 61,
    "overdue_days_end": 90,
    "sort_order": 3,
    "is_active": true
  }'
```

**测试步骤 - 创建M4+队列**:
```bash
curl -X POST http://localhost:8000/api/v1/queues \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_id": 1,
    "queue_code": "M4",
    "queue_name": "M4+队列（91-120天）",
    "queue_name_en": "M4+ Queue (91-120 days)",
    "queue_description": "逾期91-120天的案件",
    "overdue_days_start": 91,
    "overdue_days_end": 120,
    "sort_order": 4,
    "is_active": true
  }'
```

**测试步骤 - 创建LEGAL队列**:
```bash
curl -X POST http://localhost:8000/api/v1/queues \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_id": 1,
    "queue_code": "LEGAL",
    "queue_name": "法务队列（120天以上）",
    "queue_name_en": "Legal Queue (120+ days)",
    "queue_description": "逾期120天以上的案件，需要法务处理",
    "overdue_days_start": 121,
    "overdue_days_end": null,
    "sort_order": 5,
    "is_active": true
  }'
```

**验证点**:
- ✅ 5个队列全部创建成功
- ✅ 逾期天数范围不重叠
- ✅ queue_code 在同一甲方内唯一

### 2.4 创建小组群（Team Group）

**测试目的**: 为机构1创建2个小组群

**前置条件**: 机构1已创建（agency_id = 1）

**测试步骤 - 创建小组群1**:
```bash
curl -X POST http://localhost:8000/api/v1/team-groups \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_id": 1,
    "agency_id": 1,
    "group_code": "GROUP_A",
    "group_name": "A组群",
    "group_name_en": "Group A",
    "description": "负责M1和M2队列",
    "sort_order": 1,
    "is_active": true
  }'
```

**测试步骤 - 创建小组群2**:
```bash
curl -X POST http://localhost:8000/api/v1/team-groups \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_id": 1,
    "agency_id": 1,
    "group_code": "GROUP_B",
    "group_name": "B组群",
    "group_name_en": "Group B",
    "description": "负责M3+和M4+队列",
    "sort_order": 2,
    "is_active": true
  }'
```

**预期结果**: 返回小组群ID

**验证点**:
- ✅ 小组群成功关联到机构
- ✅ group_code 在同一机构内唯一

### 2.5 创建催收小组（Team）

**测试目的**: 为小组群A创建3个小组（M1组、M2组、综合组）

**前置条件**: 
- 小组群A已创建（team_group_id = 1）
- M1、M2队列已创建

**测试步骤 - 创建M1小组**:
```bash
curl -X POST http://localhost:8000/api/v1/teams \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_id": 1,
    "agency_id": 1,
    "team_group_id": 1,
    "queue_id": 1,
    "team_code": "TEAM_M1",
    "team_name": "M1催收小组",
    "team_name_en": "M1 Collection Team",
    "team_type": "电催组",
    "description": "专门负责M1队列（1-30天）",
    "max_case_count": 5000,
    "sort_order": 1,
    "is_active": true
  }'
```

**测试步骤 - 创建M2小组**:
```bash
curl -X POST http://localhost:8000/api/v1/teams \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_id": 1,
    "agency_id": 1,
    "team_group_id": 1,
    "queue_id": 2,
    "team_code": "TEAM_M2",
    "team_name": "M2催收小组",
    "team_name_en": "M2 Collection Team",
    "team_type": "电催组",
    "description": "专门负责M2队列（31-60天）",
    "max_case_count": 3000,
    "sort_order": 2,
    "is_active": true
  }'
```

**测试步骤 - 创建综合小组**:
```bash
curl -X POST http://localhost:8000/api/v1/teams \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_id": 1,
    "agency_id": 1,
    "team_group_id": 1,
    "queue_id": 1,
    "team_code": "TEAM_GENERAL",
    "team_name": "综合催收小组",
    "team_name_en": "General Collection Team",
    "team_type": "综合组",
    "description": "综合处理各类队列",
    "max_case_count": 2000,
    "sort_order": 3,
    "is_active": true
  }'
```

**验证点**:
- ✅ 小组成功关联到小组群和队列
- ✅ team_code 在同一机构内唯一
- ✅ 每个小组必须关联一个队列

### 2.6 创建催员（Collector）

**测试目的**: 为M1小组创建3名催员

**前置条件**: M1小组已创建（team_id = 1）

**测试步骤 - 创建催员1（组长）**:
```bash
curl -X POST http://localhost:8000/api/v1/collectors \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_id": 1,
    "agency_id": 1,
    "team_id": 1,
    "collector_code": "COL_001",
    "collector_name": "王小明",
    "login_id": "collector001",
    "password": "123456",
    "mobile": "+63-917-123-4567",
    "email": "collector001@example.com",
    "employee_no": "EMP001",
    "collector_level": "高级",
    "max_case_count": 150,
    "specialties": ["高额案件", "法务处理"],
    "status": "active",
    "is_active": true
  }'
```

**测试步骤 - 创建催员2**:
```bash
curl -X POST http://localhost:8000/api/v1/collectors \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_id": 1,
    "agency_id": 1,
    "team_id": 1,
    "collector_code": "COL_002",
    "collector_name": "李小红",
    "login_id": "collector002",
    "password": "123456",
    "mobile": "+63-917-234-5678",
    "email": "collector002@example.com",
    "employee_no": "EMP002",
    "collector_level": "中级",
    "max_case_count": 120,
    "specialties": ["电话催收"],
    "status": "active",
    "is_active": true
  }'
```

**测试步骤 - 创建催员3**:
```bash
curl -X POST http://localhost:8000/api/v1/collectors \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_id": 1,
    "agency_id": 1,
    "team_id": 1,
    "collector_code": "COL_003",
    "collector_name": "张小刚",
    "login_id": "collector003",
    "password": "123456",
    "mobile": "+63-917-345-6789",
    "email": "collector003@example.com",
    "employee_no": "EMP003",
    "collector_level": "初级",
    "max_case_count": 100,
    "specialties": ["WhatsApp催收"],
    "status": "active",
    "is_active": true
  }'
```

**验证点**:
- ✅ 催员成功创建并关联到小组
- ✅ login_id 全局唯一
- ✅ 密码已加密存储
- ✅ 催员可以登录IM端

### 2.7 设置小组组长

**测试步骤**:
```bash
curl -X PUT http://localhost:8000/api/v1/teams/1 \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "team_leader_id": 1
  }'
```

**预期结果**: M1小组的组长设置为催员1（王小明）

---

## 📊 三、字段配置测试

### 3.1 查看标准字段

**测试步骤**:
```bash
curl http://localhost:8000/api/v1/fields/standard \
  -H "Authorization: Bearer {superadmin_token}"
```

**预期结果**: 返回所有标准字段列表（应该已有初始数据）

**验证点**:
- ✅ 包含客户基础信息字段
- ✅ 包含贷款详情字段
- ✅ 包含还款记录字段
- ✅ 字段按分组和排序显示

### 3.2 甲方字段展示配置

**测试目的**: 为甲方A配置案件列表展示字段

**测试步骤**:
```bash
curl -X POST http://localhost:8000/api/v1/field-display/batch \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_id": 1,
    "scene_type": "admin_case_list",
    "fields": [
      {
        "field_key": "case_code",
        "field_name": "案件编号",
        "field_data_type": "String",
        "field_source": "standard",
        "sort_order": 1,
        "display_width": 150,
        "color_type": "normal",
        "is_searchable": true
      },
      {
        "field_key": "user_name",
        "field_name": "客户姓名",
        "field_data_type": "String",
        "field_source": "standard",
        "sort_order": 2,
        "display_width": 120,
        "color_type": "normal",
        "is_searchable": true
      },
      {
        "field_key": "mobile",
        "field_name": "手机号",
        "field_data_type": "String",
        "field_source": "standard",
        "sort_order": 3,
        "display_width": 130,
        "color_type": "normal",
        "is_searchable": true
      },
      {
        "field_key": "overdue_days",
        "field_name": "逾期天数",
        "field_data_type": "Integer",
        "field_source": "standard",
        "sort_order": 4,
        "display_width": 100,
        "color_type": "red",
        "is_range_searchable": true
      },
      {
        "field_key": "outstanding_amount",
        "field_name": "逾期金额",
        "field_data_type": "Decimal",
        "field_source": "standard",
        "sort_order": 5,
        "display_width": 120,
        "color_type": "red",
        "is_range_searchable": true
      }
    ]
  }'
```

**验证点**:
- ✅ 字段配置成功保存
- ✅ 字段按 sort_order 排序
- ✅ 可搜索字段标记正确

---

## 📦 四、案件数据导入测试

### 4.1 从甲方接收案件数据

**测试目的**: 模拟甲方核心系统推送案件数据

**前置条件**: 甲方A、机构、小组、催员均已创建

**测试步骤 - 导入10个案件**:
```bash
curl -X POST http://localhost:8000/api/v1/cases/import \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "tenant_id": 1,
    "cases": [
      {
        "case_code": "CASE_001",
        "user_id": "USER_001",
        "user_name": "陈大明",
        "mobile": "+63-917-111-1111",
        "overdue_days": 15,
        "loan_amount": 5000.00,
        "repaid_amount": 1000.00,
        "outstanding_amount": 4000.00,
        "due_date": "2025-11-05T00:00:00",
        "case_status": "pending_repayment"
      },
      {
        "case_code": "CASE_002",
        "user_id": "USER_002",
        "user_name": "黄小华",
        "mobile": "+63-917-222-2222",
        "overdue_days": 25,
        "loan_amount": 8000.00,
        "repaid_amount": 0.00,
        "outstanding_amount": 8000.00,
        "due_date": "2025-10-26T00:00:00",
        "case_status": "pending_repayment"
      },
      {
        "case_code": "CASE_003",
        "user_id": "USER_003",
        "user_name": "林小丽",
        "mobile": "+63-917-333-3333",
        "overdue_days": 45,
        "loan_amount": 12000.00,
        "repaid_amount": 3000.00,
        "outstanding_amount": 9000.00,
        "due_date": "2025-10-06T00:00:00",
        "case_status": "partial_repayment"
      },
      {
        "case_code": "CASE_004",
        "user_id": "USER_004",
        "user_name": "吴小强",
        "mobile": "+63-917-444-4444",
        "overdue_days": 55,
        "loan_amount": 15000.00,
        "repaid_amount": 0.00,
        "outstanding_amount": 15000.00,
        "due_date": "2025-09-26T00:00:00",
        "case_status": "pending_repayment"
      },
      {
        "case_code": "CASE_005",
        "user_id": "USER_005",
        "user_name": "郑小美",
        "mobile": "+63-917-555-5555",
        "overdue_days": 8,
        "loan_amount": 3000.00,
        "repaid_amount": 500.00,
        "outstanding_amount": 2500.00,
        "due_date": "2025-11-12T00:00:00",
        "case_status": "partial_repayment"
      },
      {
        "case_code": "CASE_006",
        "user_id": "USER_006",
        "user_name": "周小刚",
        "mobile": "+63-917-666-6666",
        "overdue_days": 35,
        "loan_amount": 10000.00,
        "repaid_amount": 0.00,
        "outstanding_amount": 10000.00,
        "due_date": "2025-10-16T00:00:00",
        "case_status": "pending_repayment"
      },
      {
        "case_code": "CASE_007",
        "user_id": "USER_007",
        "user_name": "钱小芳",
        "mobile": "+63-917-777-7777",
        "overdue_days": 18,
        "loan_amount": 6000.00,
        "repaid_amount": 2000.00,
        "outstanding_amount": 4000.00,
        "due_date": "2025-11-02T00:00:00",
        "case_status": "partial_repayment"
      },
      {
        "case_code": "CASE_008",
        "user_id": "USER_008",
        "user_name": "孙小明",
        "mobile": "+63-917-888-8888",
        "overdue_days": 70,
        "loan_amount": 20000.00,
        "repaid_amount": 0.00,
        "outstanding_amount": 20000.00,
        "due_date": "2025-09-11T00:00:00",
        "case_status": "pending_repayment"
      },
      {
        "case_code": "CASE_009",
        "user_id": "USER_009",
        "user_name": "赵小红",
        "mobile": "+63-917-999-9999",
        "overdue_days": 12,
        "loan_amount": 4000.00,
        "repaid_amount": 1000.00,
        "outstanding_amount": 3000.00,
        "due_date": "2025-11-08T00:00:00",
        "case_status": "partial_repayment"
      },
      {
        "case_code": "CASE_010",
        "user_id": "USER_010",
        "user_name": "刘小军",
        "mobile": "+63-917-000-0000",
        "overdue_days": 5,
        "loan_amount": 2000.00,
        "repaid_amount": 0.00,
        "outstanding_amount": 2000.00,
        "due_date": "2025-11-15T00:00:00",
        "case_status": "pending_repayment"
      }
    ]
  }'
```

**预期结果**: 
```json
{
  "code": 200,
  "message": "导入成功",
  "data": {
    "total": 10,
    "success": 10,
    "failed": 0
  }
}
```

**验证点**:
- ✅ 10个案件全部导入成功
- ✅ case_code 唯一
- ✅ 案件根据 overdue_days 自动分配到对应队列

### 4.2 验证案件自动分队列

**测试步骤**:
```bash
# 查询M1队列的案件（1-30天）
curl "http://localhost:8000/api/v1/cases?queue_id=1" \
  -H "Authorization: Bearer {superadmin_token}"
```

**预期结果**: 应该包含 CASE_001, CASE_002, CASE_005, CASE_007, CASE_009, CASE_010

```bash
# 查询M2队列的案件（31-60天）
curl "http://localhost:8000/api/v1/cases?queue_id=2" \
  -H "Authorization: Bearer {superadmin_token}"
```

**预期结果**: 应该包含 CASE_003, CASE_004, CASE_006

```bash
# 查询M3+队列的案件（61-90天）
curl "http://localhost:8000/api/v1/cases?queue_id=3" \
  -H "Authorization: Bearer {superadmin_token}"
```

**预期结果**: 应该包含 CASE_008

---

## 🎯 五、案件分配测试

### 5.1 手动分配案件给催员

**测试目的**: 将案件分配给催员

**测试步骤 - 分配CASE_001给催员1**:
```bash
curl -X POST http://localhost:8000/api/v1/cases/CASE_001/assign \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "collector_id": 1,
    "reason": "手动分配"
  }'
```

**预期结果**:
```json
{
  "code": 200,
  "message": "分配成功",
  "data": {
    "case_id": 1,
    "case_code": "CASE_001",
    "collector_id": 1,
    "collector_name": "王小明",
    "assigned_at": "2025-11-20 21:30:00"
  }
}
```

**验证点**:
- ✅ 案件成功分配给催员
- ✅ 生成案件分配历史记录
- ✅ 催员当前案件数量 +1

### 5.2 批量自动分案

**测试目的**: 将M1队列的未分配案件自动分配

**测试步骤**:
```bash
curl -X POST http://localhost:8000/api/v1/cases/auto-assign \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "team_id": 1,
    "queue_id": 1,
    "strategy": "balanced"
  }'
```

**预期结果**:
```json
{
  "code": 200,
  "message": "自动分案完成",
  "data": {
    "total_cases": 5,
    "assigned": 5,
    "failed": 0,
    "assignments": [
      {"collector_id": 1, "case_count": 2},
      {"collector_id": 2, "case_count": 2},
      {"collector_id": 3, "case_count": 1}
    ]
  }
}
```

**验证点**:
- ✅ 案件均匀分配给催员
- ✅ 不超过催员的 max_case_count
- ✅ 记录分配历史

---

## 📱 六、IM端催员操作测试

### 6.1 催员登录IM端

**测试步骤**:
```bash
curl -X POST http://localhost:8000/api/v1/im/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "loginId": "collector001",
    "password": "123456"
  }'
```

**预期结果**: 返回催员的 token 和基本信息

### 6.2 查看我的案件列表

**测试步骤**:
```bash
curl "http://localhost:8000/api/v1/im/cases/my" \
  -H "Authorization: Bearer {collector_token}"
```

**预期结果**: 返回分配给该催员的所有案件

**验证点**:
- ✅ 只返回该催员的案件
- ✅ 包含案件基本信息和字段值
- ✅ 按逾期天数倒序排列

### 6.3 查看案件详情

**测试步骤**:
```bash
curl "http://localhost:8000/api/v1/im/cases/CASE_001" \
  -H "Authorization: Bearer {collector_token}"
```

**预期结果**: 返回案件完整信息

**验证点**:
- ✅ 包含客户基础信息
- ✅ 包含贷款详情
- ✅ 包含还款记录
- ✅ 包含沟通历史

### 6.4 添加沟通记录

**测试步骤**:
```bash
curl -X POST http://localhost:8000/api/v1/im/cases/CASE_001/communication \
  -H "Authorization: Bearer {collector_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "contact_method": "phone",
    "contact_result": "接通",
    "communication_content": "客户表示下周一还款",
    "next_follow_up_at": "2025-11-25T10:00:00"
  }'
```

**预期结果**:
```json
{
  "code": 200,
  "message": "沟通记录保存成功",
  "data": {
    "id": 1,
    "case_id": 1,
    "collector_id": 1,
    "contact_method": "phone",
    "contact_result": "接通",
    "created_at": "2025-11-20 21:35:00"
  }
}
```

### 6.5 添加PTP承诺

**测试步骤**:
```bash
curl -X POST http://localhost:8000/api/v1/im/cases/CASE_001/ptp \
  -H "Authorization: Bearer {collector_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "promise_amount": 2000.00,
    "promise_date": "2025-11-25",
    "promise_method": "bank_transfer",
    "notes": "客户承诺下周一还款2000"
  }'
```

**预期结果**: PTP记录创建成功

### 6.6 更新案件状态

**测试步骤 - 标记为部分还款**:
```bash
curl -X PUT http://localhost:8000/api/v1/im/cases/CASE_001 \
  -H "Authorization: Bearer {collector_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "case_status": "partial_repayment",
    "repaid_amount": 3000.00
  }'
```

**预期结果**: 案件状态更新成功

---

## 🔔 七、通知系统测试

### 7.1 创建通知模板

**测试目的**: 创建案件标签变更通知模板

**测试步骤**:
```bash
curl -X POST http://localhost:8000/api/v1/notification-templates \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "template_id": "CASE_TAG_CHANGE",
    "template_name": "案件标签变更通知",
    "template_type": "case_tag_change",
    "content_template": "案件 {case_code} 的标签已变更为 {tag_name}",
    "target_type": "collector",
    "is_forced_read": false,
    "repeat_interval_minutes": 30,
    "max_remind_count": 3,
    "priority": "medium",
    "display_duration_seconds": 5,
    "is_enabled": true
  }'
```

**验证点**:
- ✅ 模板创建成功
- ✅ template_id 唯一

### 7.2 推送通知

**测试步骤**:
```bash
curl -X POST http://localhost:8000/api/v1/notifications/push \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "template_id": "CASE_TAG_CHANGE",
    "target_collectors": [1, 2, 3],
    "variables": {
      "case_code": "CASE_001",
      "tag_name": "高优先级"
    }
  }'
```

**预期结果**: 通知推送成功

### 7.3 催员接收通知

**测试步骤**:
```bash
curl "http://localhost:8000/api/v1/im/notifications/unread" \
  -H "Authorization: Bearer {collector_token}"
```

**预期结果**: 返回该催员的未读通知列表

### 7.4 标记通知已读

**测试步骤**:
```bash
curl -X POST http://localhost:8000/api/v1/im/notifications/1/read \
  -H "Authorization: Bearer {collector_token}"
```

**预期结果**: 通知标记为已读

---

## 📈 八、数据看板测试

### 8.1 查看沟通记录统计

**测试步骤**:
```bash
curl "http://localhost:8000/api/v1/dashboard/communications?date=2025-11-20" \
  -H "Authorization: Bearer {superadmin_token}"
```

**预期结果**:
```json
{
  "code": 200,
  "data": {
    "total_communications": 10,
    "by_method": {
      "phone": 6,
      "whatsapp": 3,
      "sms": 1
    },
    "by_result": {
      "接通": 5,
      "未接通": 3,
      "关机": 2
    }
  }
}
```

### 8.2 查看PTP统计

**测试步骤**:
```bash
curl "http://localhost:8000/api/v1/dashboard/ptp?date=2025-11-20" \
  -H "Authorization: Bearer {superadmin_token}"
```

**预期结果**: 返回PTP承诺统计数据

### 8.3 查看催员绩效

**测试步骤**:
```bash
curl "http://localhost:8000/api/v1/dashboard/performance?collector_id=1&month=2025-11" \
  -H "Authorization: Bearer {superadmin_token}"
```

**预期结果**:
```json
{
  "code": 200,
  "data": {
    "collector_id": 1,
    "collector_name": "王小明",
    "total_cases": 50,
    "contacted_cases": 45,
    "resolved_cases": 10,
    "contact_rate": 0.90,
    "resolution_rate": 0.20,
    "total_collected_amount": 50000.00
  }
}
```

### 8.4 查看空闲催员监控

**测试步骤**:
```bash
curl "http://localhost:8000/api/v1/idle-monitor/calculate" \
  -H "Authorization: Bearer {superadmin_token}" \
  -H "Content-Type: application/json" \
  -d '{
    "date": "2025-11-20",
    "agency_id": 1
  }'
```

**预期结果**: 返回催员空闲时间统计

---

## ✅ 九、完整性验证测试

### 9.1 数据完整性验证

**验证步骤**:
1. 验证所有甲方都有机构
2. 验证所有机构都有小组
3. 验证所有小组都有催员
4. 验证所有案件都分配到队列
5. 验证所有催员案件数不超过上限

**SQL验证**:
```sql
-- 验证甲方-机构关联
SELECT t.tenant_name, COUNT(a.id) as agency_count 
FROM tenants t 
LEFT JOIN collection_agencies a ON t.id = a.tenant_id 
GROUP BY t.id;

-- 验证案件分配情况
SELECT q.queue_name, COUNT(c.id) as case_count 
FROM case_queues q 
LEFT JOIN cases c ON q.id = c.queue_id 
GROUP BY q.id;

-- 验证催员案件数
SELECT col.collector_name, COUNT(c.id) as current_cases, col.max_case_count
FROM collectors col
LEFT JOIN cases c ON col.id = c.collector_id
GROUP BY col.id;
```

### 9.2 权限验证

**测试场景**:
1. SuperAdmin 可以访问所有API
2. TenantAdmin 只能访问自己甲方的数据
3. Collector 只能访问自己的案件

**测试步骤 - 催员尝试访问其他人的案件**:
```bash
curl "http://localhost:8000/api/v1/im/cases/CASE_010" \
  -H "Authorization: Bearer {collector001_token}"
```

**预期结果**: 如果CASE_010不属于该催员，应返回403或404

---

## 📝 十、测试报告模板

### 测试执行记录

| 测试模块 | 测试用例数 | 通过 | 失败 | 跳过 | 通过率 |
|---------|-----------|------|------|------|--------|
| 认证授权 | 4 | | | | |
| 基础数据创建 | 7 | | | | |
| 字段配置 | 2 | | | | |
| 案件导入 | 2 | | | | |
| 案件分配 | 2 | | | | |
| IM端操作 | 6 | | | | |
| 通知系统 | 4 | | | | |
| 数据看板 | 4 | | | | |
| 完整性验证 | 2 | | | | |
| **总计** | **33** | | | | |

### 测试环境

- 测试日期: ____年__月__日
- 测试人员: _________
- 后端版本: v1.0.0
- 数据库: SQLite/MySQL
- 测试数据: 初始化完成

### 发现的问题

| 问题ID | 问题描述 | 严重程度 | 状态 | 备注 |
|-------|---------|---------|------|------|
| | | | | |

### 测试结论

- [ ] 所有核心功能测试通过
- [ ] 性能测试通过
- [ ] 安全测试通过
- [ ] 系统可以上线

---

**测试完成！** 🎉

本测试用例覆盖了 CCO 系统从基础数据创建到业务流程的完整功能验证。

