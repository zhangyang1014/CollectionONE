# 上传后自动对比功能说明

## 📋 功能概述

"上传后显示与上一版本的对比"是案件列表甲方字段上传功能的增强特性，帮助用户在上传新版本后立即了解与上一版本的差异。

## 🎯 功能定位

### 使用场景
- ✅ **配置变更审核**：上传前确认修改内容是否正确
- ✅ **版本变更说明**：快速生成变更报告供甲方确认
- ✅ **多人协作**：了解其他管理员的配置变更
- ✅ **问题排查**：对比新旧版本定位配置问题

### 核心价值
1. **即时反馈**：上传完成立即展示差异，无需手动触发
2. **变更可视化**：清晰展示新增、删除、修改的字段
3. **决策支持**：基于对比结果决定是否保留新版本
4. **文档化**：一键导出对比报告，留存变更记录

## 🔧 功能详解

### 1. 触发机制

#### 触发条件
```
✅ 用户勾选"上传后显示与上一版本的对比"选项
AND
✅ 文件上传成功
AND
✅ 新版本已创建
AND
✅ 系统中存在至少2个版本（非首次上传）
```

#### 执行时序
```
用户点击"确认上传"
↓
文件格式验证（如勾选）
↓
创建新版本（如：版本4）
↓
设为当前版本（如勾选）
↓
检查是否勾选"显示对比"
↓
【是】调用对比接口（版本3 vs 版本4）
↓
关闭上传弹窗
↓
自动弹出对比结果弹窗（200ms延迟）
↓
展示对比详情
```

### 2. 对比内容

#### 变更类型

| 类型 | 标识 | 说明 | 示例 |
|------|------|------|------|
| **新增字段** | 🟢 绿色 | 在版本4中新增，版本3中不存在 | 新增"风险等级"字段 |
| **删除字段** | 🔴 红色 | 在版本3中存在，版本4中删除 | 删除"备注"字段 |
| **修改字段** | 🟡 黄色 | 字段存在但属性发生变化 | "案件状态"枚举值新增"已关闭" |
| **未变更** | ⚪ 灰色 | 两个版本完全一致的字段 | 其余13个字段 |

#### 对比维度

| 属性 | 字段名 | 对比说明 |
|------|--------|---------|
| 字段名称 | `field_name` | 中文显示名称 |
| 字段类型 | `field_type` | String, Number, Enum等 |
| 枚举值 | `enum_values` | 仅Enum类型有效 |
| 是否必填 | `is_required` | true/false |
| 排序 | `sort_order` | 数值排序 |
| 描述 | `description` | 字段说明文本 |

### 3. UI界面

#### 弹窗布局
```
┌──────────────────────────────────────────────────────────────┐
│  自动版本对比：版本3 vs 版本4（新上传）                 [×]   │
├──────────────────────────────────────────────────────────────┤
│  💡 这是您刚刚上传的新版本与上一版本的对比结果                │
│                                                               │
│  版本3（上一版本）              版本4（新上传，当前使用）      │
│  2025-12-07 15:30 | 14字段      2025-12-08 10:00 | 16字段    │
│                                                               │
│  📊 变更摘要                                                  │
│  • 新增字段：2个                                              │
│  • 删除字段：0个                                              │
│  • 修改字段：1个                                              │
│  • 未变更：13个                                               │
│                                                               │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                               │
│  📝 详细对比                                                  │
│                                                               │
│  🟢 新增字段（2个）                                           │
│  + 风险等级 (risk_level) - Enum | 必填:否                     │
│  + 借款渠道 (loan_channel) - String | 必填:否                 │
│                                                               │
│  🟡 修改字段（1个）                                           │
│  ≈ 案件状态 (case_status)                                     │
│    枚举值: [待分配,催收中,已完成] → [待分配,催收中,已完成,已关闭] │
│                                                               │
│  ⚪ 未变更字段（13个）                          [展开/收起]   │
│                                                               │
│  [导出对比报告]  [下载版本3]  [下载版本4]  [确认并关闭]       │
└──────────────────────────────────────────────────────────────┘
```

#### 视觉特性
- ✅ 弹窗宽度：90%（最大1200px）
- ✅ 高度：自适应内容，最大80vh，超出可滚动
- ✅ 新版本卡片：淡蓝色背景高亮
- ✅ 提示信息：蓝色info样式
- ✅ 按钮文案：主按钮为"确认并关闭"

### 4. 用户操作

#### 可用操作

| 操作 | 说明 | 快捷键 |
|------|------|--------|
| **浏览对比** | 上下滚动查看完整对比内容 | 鼠标滚轮 |
| **展开/折叠** | 查看未变更字段详情 | 点击标题 |
| **导出报告** | 生成Markdown格式对比报告 | - |
| **下载版本** | 下载任一版本的JSON文件 | - |
| **关闭弹窗** | 返回字段列表页面 | ESC或点击×或确认按钮 |

#### 导出报告格式
```markdown
# 版本对比报告（新上传）

## 基本信息
- 对比版本：版本3 vs 版本4（新上传）
- 对比时间：2025-12-08 10:00:00
- 操作人：管理员 (admin)

## 版本信息
| 项目 | 版本3 | 版本4（新上传） |
|------|-------|----------------|
| 上传时间 | 2025-12-07 15:30:00 | 2025-12-08 10:00:00 |
| 字段数 | 14个 | 16个 |

## 变更摘要
- 新增字段：2个
- 删除字段：0个
- 修改字段：1个
- 未变更：13个

## 详细对比
### 新增字段
1. 风险等级 (risk_level)
   - 类型：Enum
   - 必填：否
   - 枚举值：[低风险, 中风险, 高风险]

2. 借款渠道 (loan_channel)
   - 类型：String
   - 必填：否

### 修改字段
1. 案件状态 (case_status)
   - 枚举值：[待分配, 催收中, 已完成] → [待分配, 催收中, 已完成, 已关闭]

---
生成时间：2025-12-08 10:00:00
```

### 5. 异常处理

#### 异常场景及处理

| 异常情况 | 处理方式 | 用户提示 |
|---------|---------|---------|
| **首次上传**（仅1个版本） | 不触发对比 | "这是首次上传，无历史版本可对比" |
| **上一版本数据缺失** | 显示错误，不影响上传 | "无法获取上一版本数据，对比失败" |
| **对比接口调用失败** | 显示警告，不影响上传 | "自动对比失败，您可在版本管理中手动查看" |
| **对比计算超时**（>5秒） | 显示警告提示 | "对比计算中，请稍后手动查看" |
| **网络断开** | 后台重试3次，失败则提示 | "网络异常，对比失败" |

#### 容错设计
```typescript
// 对比失败不影响上传成功
try {
  await showAutoCompare()
} catch (error) {
  // 静默处理，只显示提示
  ElMessage.warning('自动对比失败，您可稍后手动查看')
  // 记录日志便于排查
  logger.warn('Auto compare failed', { error, version })
}
```

## 💡 最佳实践

### 建议勾选的场景
✅ **重要配置变更**
- 新增或删除核心字段
- 修改枚举值选项
- 调整必填字段设置

✅ **多人协作环境**
- 需要了解其他管理员的变更
- 需要向甲方汇报配置变更

✅ **生产环境上线前**
- 最终确认配置是否正确
- 生成变更说明文档

### 不建议勾选的场景
❌ **首次上传**
- 系统中无历史版本，无法对比

❌ **频繁测试**
- 短时间内多次上传测试
- 避免频繁弹窗影响体验

❌ **微小调整**
- 仅修改字段描述文字
- 调整排序等次要属性

❌ **网络不稳定**
- 移动网络或弱网环境
- 可能导致对比超时

## 📊 性能指标

### 性能要求

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 对比计算时间 | < 1秒 | 100个字段以内 |
| 对比弹窗加载 | < 500ms | 首次渲染 |
| 报告导出时间 | < 2秒 | Markdown生成 |
| 接口响应时间 | < 800ms | 对比API调用 |

### 性能优化措施
1. **后台异步计算**：对比在后台进行，不阻塞UI
2. **结果缓存**：对比结果临时缓存5分钟
3. **懒加载**：未变更字段默认折叠，点击才渲染
4. **虚拟滚动**：超过100个字段时使用虚拟列表

## 🔍 技术实现

### 前端关键代码

```typescript
// 上传成功后触发
async function handleUploadSuccess(result) {
  const { version, fields_count } = result
  
  // 1. 显示成功提示
  ElMessage.success(`成功上传版本${version}，共 ${fields_count} 个字段`)
  
  // 2. 关闭上传弹窗
  uploadDialogVisible.value = false
  
  // 3. 刷新字段列表
  await loadTenantFields()
  
  // 4. 如果勾选了对比选项
  if (uploadOptions.showCompare && version > 1) {
    // 延迟200ms让用户看到成功提示
    await sleep(200)
    
    // 自动触发对比
    await showAutoCompare(version)
  }
}

// 自动对比函数
async function showAutoCompare(newVersion) {
  try {
    const prevVersion = newVersion - 1
    
    // 调用对比接口
    const result = await compareVersions(tenantId, prevVersion, newVersion, 'list')
    
    // 设置对比数据
    compareData.value = result
    isAutoCompare.value = true
    
    // 显示对比弹窗
    compareDialogVisible.value = true
    
  } catch (error) {
    ElMessage.warning('自动对比失败，您可在版本管理中手动查看')
    console.error('Auto compare error:', error)
  }
}
```

### 后端接口

```
GET /api/v1/tenants/{tenantId}/fields-json/compare

参数：
- scene: list
- version1: 3  (上一版本)
- version2: 4  (新上传的版本)

响应：
{
  "version1": { "version": 3, "uploaded_at": "...", "fields_count": 14 },
  "version2": { "version": 4, "uploaded_at": "...", "fields_count": 16 },
  "summary": { "added": 2, "removed": 0, "modified": 1, "unchanged": 13 },
  "details": {
    "added": [...],
    "removed": [...],
    "modified": [...],
    "unchanged": [...]
  }
}
```

## 📈 数据埋点

### 关键指标

| 指标 | 说明 | 用途 |
|------|------|------|
| 勾选率 | 上传时勾选对比选项的比例 | 评估功能使用频率 |
| 触发次数 | 自动对比功能被触发的次数 | 统计实际使用量 |
| 成功率 | 对比接口调用成功率 | 监控系统稳定性 |
| 报告导出量 | 对比报告被导出的次数 | 评估功能价值 |
| 停留时长 | 用户在对比弹窗的停留时间 | 评估内容质量 |

### 埋点事件

```javascript
// 勾选对比选项
track('upload_compare_option_checked', { tenant_id, user_id })

// 自动对比触发
track('auto_compare_triggered', { tenant_id, old_version, new_version })

// 对比成功
track('auto_compare_success', { tenant_id, version, duration })

// 对比失败
track('auto_compare_failed', { tenant_id, error_type })

// 导出报告
track('compare_report_exported', { tenant_id, version })
```

## 🎓 用户引导

### 首次使用提示

```
┌──────────────────────────────────────────────────────┐
│  💡 新功能提示                                   [×]   │
├──────────────────────────────────────────────────────┤
│  上传后可自动对比版本差异！                            │
│                                                       │
│  勾选"上传后显示与上一版本的对比"选项，              │
│  上传完成后将自动展示：                                │
│  ✓ 新增了哪些字段                                    │
│  ✓ 删除了哪些字段                                    │
│  ✓ 修改了哪些字段                                    │
│                                                       │
│  还可以一键导出对比报告哦！                            │
│                                                       │
│  [ 不再提示 ]                             [ 我知道了 ] │
└──────────────────────────────────────────────────────┘
```

## 📚 相关文档

- [案件列表甲方字段查看PRD](../../PRD需求文档/CCO管理控台/字段管理/2-案件列表甲方字段查看-PRD.md) - 第7.2.3节和第10.5.2节
- [版本对比功能PRD](../../PRD需求文档/CCO管理控台/字段管理/2-案件列表甲方字段查看-PRD.md) - 第8.3节
- [后端API文档](../../PRD需求文档/CCO管理控台/字段管理/2-案件列表甲方字段查看-PRD.md) - 第11.2.4节

---

**最后更新**：2025-12-08  
**文档版本**：1.0.0  
**维护人**：产品团队
