# 消息模板变量列表更新说明

## 📋 更新时间
2025-12-03

## 🎯 更新目标

根据用户需求，更新消息模板配置管理功能中的可用变量列表，**所有变量值来源于"甲方字段展示配置"中定义的标准字段**。

---

## 📊 新的变量列表

### 变量对照表

| 变量名 | 变量标识 | 字段标识(field_key) | 数据类型 | 示例值 | 说明 |
|--------|---------|-------------------|---------|--------|------|
| 客户名 | `{客户名}` | user_name | String | 张三 | 客户真实姓名 |
| 贷款编号 | `{贷款编号}` | case_code | String | BTSK-200100 | 案件编号 |
| 逾期天数 | `{逾期天数}` | overdue_days | Integer | 23 | 当前逾期天数 |
| 到期日期 | `{到期日期}` | due_date | Date | 2025-01-15 | 应还款日期 |
| 贷款金额 | `{贷款金额}` | loan_amount | Decimal | 50,000 | 原始贷款金额 |
| 应还金额 | `{应还金额}` | outstanding_amount | Decimal | 10,529 | 应还未还金额 |
| 本金 | `{本金}` | principal_amount | Decimal | 50,000 | 贷款本金 |
| 罚息 | `{罚息}` | late_fee | Decimal | 529 | 逾期罚息 |
| 产品名称 | `{产品名称}` | product_name | String | 快速贷 | 贷款产品名称 |
| App名称 | `{App名称}` | app_name | String | MegaPeso | 借款App名称 |

### 对比旧版本

**旧版本变量**（已移除）：
- `{还款日期}` → 已替换为 `{到期日期}`
- `{联系电话}` → 已移除（不属于标准字段）
- `{机构名称}` → 已移除（不属于标准字段）
- `{催员姓名}` → 已移除（不属于标准字段）

**新增变量**：
- `{到期日期}` - 应还款日期
- `{贷款金额}` - 原始贷款金额
- `{本金}` - 贷款本金
- `{罚息}` - 逾期罚息
- `{产品名称}` - 贷款产品名称
- `{App名称}` - 借款App名称

---

## 🔧 实施内容

### 1. PRD文档更新

**文件**: `PRD需求文档/CCO管理控台/消息模板配置管理PRD.md`

**更新内容**:
1. 变量列表表格增加 `字段标识(field_key)` 列
2. 更新所有变量的定义和示例值
3. 更新变量替换逻辑的伪代码
4. 添加数据来源说明："变量值来自甲方字段展示配置中定义的标准字段"

**关键代码示例**:
```javascript
function replaceVariables(templateContent, caseData) {
  let content = templateContent
  
  // 替换客户名
  content = content.replace(/{客户名}/g, caseData.user_name || '{客户名}')
  
  // 替换贷款金额（需要格式化）
  const loanAmount = caseData.loan_amount ? formatCurrency(caseData.loan_amount) : '{贷款金额}'
  content = content.replace(/{贷款金额}/g, loanAmount)
  
  // ... 其他变量替换
  
  return content
}
```

---

### 2. 后端API更新

**文件**: `backend-java/src/main/java/com/cco/controller/MockMessageTemplateController.java`

#### 2.1 VariableVO类增加字段

```java
@Data
public static class VariableVO {
    private String name;        // 变量名
    private String key;         // 变量标识 {变量名}
    private String fieldKey;    // 字段标识，对应甲方字段展示配置中的field_key
    private String type;        // 数据类型
    private String example;     // 示例值
    private String description; // 说明
    
    public VariableVO(String name, String key, String fieldKey, String type, String example, String description) {
        this.name = name;
        this.key = key;
        this.fieldKey = fieldKey;
        this.type = type;
        this.example = example;
        this.description = description;
    }
}
```

#### 2.2 更新getVariables()方法

```java
@GetMapping("/message-templates/variables")
public ResponseData<List<VariableVO>> getVariables() {
    List<VariableVO> variables = Arrays.asList(
        new VariableVO("客户名", "{客户名}", "user_name", "string", "张三", "客户真实姓名"),
        new VariableVO("贷款编号", "{贷款编号}", "case_code", "string", "BTSK-200100", "案件编号"),
        new VariableVO("逾期天数", "{逾期天数}", "overdue_days", "integer", "23", "当前逾期天数"),
        new VariableVO("到期日期", "{到期日期}", "due_date", "date", "2025-01-15", "应还款日期"),
        new VariableVO("贷款金额", "{贷款金额}", "loan_amount", "decimal", "50,000", "原始贷款金额"),
        new VariableVO("应还金额", "{应还金额}", "outstanding_amount", "decimal", "10,529", "应还未还金额"),
        new VariableVO("本金", "{本金}", "principal_amount", "decimal", "50,000", "贷款本金"),
        new VariableVO("罚息", "{罚息}", "late_fee", "decimal", "529", "逾期罚息"),
        new VariableVO("产品名称", "{产品名称}", "product_name", "string", "快速贷", "贷款产品名称"),
        new VariableVO("App名称", "{App名称}", "app_name", "string", "MegaPeso", "借款App名称")
    );
    
    return ResponseData.success(variables);
}
```

#### 2.3 更新Mock数据

更新了所有模板的content和variables字段，使用新的变量列表。

**示例**：
```java
t1.setContent("您好{客户名}，早上好！您在{App名称}的{产品名称}（贷款编号：{贷款编号}）已逾期{逾期天数}天，应还金额{应还金额}，请在{到期日期}前完成还款。谢谢您的配合！");
t1.setVariables(Arrays.asList("客户名", "App名称", "产品名称", "贷款编号", "逾期天数", "应还金额", "到期日期"));
```

---

### 3. 前端页面更新

**文件**: `frontend/src/views/console/MessageTemplateList.vue`

#### 3.1 更新输入框placeholder

```vue
<el-input 
  v-model="templateForm.content" 
  type="textarea"
  :rows="6"
  placeholder="请输入模板内容，可使用变量如：{客户名}、{贷款编号}、{逾期天数}、{应还金额}、{产品名称} 等"
  maxlength="1000"
  show-word-limit
/>
```

#### 3.2 更新预览数据

```typescript
const exampleData: { [key: string]: string } = {
  '{客户名}': '张三',
  '{贷款编号}': 'BTSK-200100',
  '{逾期天数}': '23',
  '{到期日期}': '2025-01-15',
  '{贷款金额}': '50,000',
  '{应还金额}': '10,529',
  '{本金}': '50,000',
  '{罚息}': '529',
  '{产品名称}': '快速贷',
  '{App名称}': 'MegaPeso'
}
```

---

### 4. 数据库脚本更新

**文件**: `backend-java/src/main/resources/db/migration/V1.0.8__create_message_templates_table.sql`

更新了所有Mock数据模板的content和variables字段。

**新增模板示例**：
```sql
(1, '贷款金额明细提醒', 'organization', NULL, 'S1-3', 'reminder', 'afternoon', 
 '{客户名}您好，您的贷款详情：贷款金额{贷款金额}，本金{本金}，罚息{罚息}，应还总额{应还金额}，请在{到期日期}前还款。', 
 '["客户名","贷款金额","本金","罚息","应还金额","到期日期"]', 1, 60, 1)
```

---

## 📝 模板内容示例

### 组织模板示例

#### 早安问候 + 还款提醒
```
您好{客户名}，早上好！您在{App名称}的{产品名称}（贷款编号：{贷款编号}）已逾期{逾期天数}天，应还金额{应还金额}，请在{到期日期}前完成还款。谢谢您的配合！
```

**使用变量**：客户名、App名称、产品名称、贷款编号、逾期天数、应还金额、到期日期

#### 下午催款提醒
```
{客户名}您好，您的{产品名称}（贷款编号：{贷款编号}）逾期已{逾期天数}天，贷款金额{贷款金额}，未还金额{应还金额}（本金{本金}+罚息{罚息}），请今日内完成还款，如有困难请联系我们！
```

**使用变量**：客户名、产品名称、贷款编号、逾期天数、贷款金额、应还金额、本金、罚息

#### 贷款金额明细提醒
```
{客户名}您好，您的贷款详情：贷款金额{贷款金额}，本金{本金}，罚息{罚息}，应还总额{应还金额}，请在{到期日期}前还款。
```

**使用变量**：客户名、贷款金额、本金、罚息、应还金额、到期日期

### 个人模板示例

#### 个人问候（上午）
```
您好{客户名}，早上好！关于您在{App名称}的{产品名称}还款事宜想跟您沟通一下，现在方便吗？
```

**使用变量**：客户名、App名称、产品名称

---

## 🔗 技术关联

### 数据流转

```
甲方字段展示配置 (tenant_field_display_configs)
    ↓
    field_key 映射
    ↓
消息模板变量系统
    ↓
    变量替换引擎
    ↓
实际发送的消息内容
```

### 关键文件映射

| 层级 | 文件 | 说明 |
|------|------|------|
| 数据层 | `tenant_field_display_configs` 表 | 存储字段配置 |
| 实体层 | `MessageTemplate.java` | 模板实体类 |
| 控制层 | `MockMessageTemplateController.java` | Mock API实现 |
| 视图层 | `MessageTemplateList.vue` | 前端页面 |
| 文档层 | `消息模板配置管理PRD.md` | 需求文档 |

---

## ✅ 验证检查点

### 后端验证
```bash
# 1. 启动Java后端
cd backend-java && ./start.sh

# 2. 测试变量列表API
curl -s "http://localhost:8080/api/v1/message-templates/variables" | jq '.'

# 预期返回：
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "name": "客户名",
      "key": "{客户名}",
      "fieldKey": "user_name",
      "type": "string",
      "example": "张三",
      "description": "客户真实姓名"
    },
    ...
  ]
}
```

### 前端验证
```bash
# 1. 启动前端
cd frontend && npm run dev

# 2. 浏览器访问
http://localhost:5173

# 3. 验证步骤
- 登录控台（超级管理员: super_admin / admin123）
- 进入"消息配置" → "消息模板配置管理"
- 点击"新增模板"
- 检查"可用变量"列表是否显示10个变量
- 在模板内容中输入变量如 {客户名}、{贷款金额} 等
- 检查"内容预览"是否正确替换为示例值
```

### 数据验证
```sql
-- 检查模板数据中的变量字段
SELECT id, template_name, variables 
FROM message_templates 
WHERE tenant_id = 1;

-- 预期结果：variables字段应包含新的变量名
```

---

## 📚 参考文档

1. **PRD文档**: `PRD需求文档/CCO管理控台/消息模板配置管理PRD.md`
2. **甲方字段配置**: `文档/功能说明/甲方字段展示配置-优化完成报告.md`
3. **标准字段定义**: `backend-java/src/main/resources/db/data/init_standard_fields.sql`
4. **实现说明**: `说明文档/功能说明/消息模板配置管理-实现说明.md`

---

## 🎯 总结

### 更新范围
- ✅ PRD文档：更新变量定义和示例
- ✅ 后端API：更新VariableVO类和getVariables()方法
- ✅ 后端Mock数据：更新8个模板的内容和变量
- ✅ 前端页面：更新预览数据和placeholder
- ✅ 数据库脚本：更新初始化数据

### 核心改进
1. **数据来源明确**：所有变量值来自"甲方字段展示配置"
2. **字段映射清晰**：通过field_key关联标准字段
3. **变量更实用**：新增贷款金额、本金、罚息等关键字段
4. **示例更真实**：模板内容更贴近实际催收场景

### 影响范围
- **不影响**现有功能运行
- **增强**模板变量的可用性
- **统一**数据来源和字段映射关系

---

**文档版本**: 1.0.0  
**更新时间**: 2025-12-03  
**维护人**: AI Assistant
















