# 重启服务指南

## 🔄 需要重启的情况

当出现以下情况时，需要重启服务：
- ✅ 安装了新的npm包（如echarts、sass）
- ✅ 修改了后端API路由（main.py）
- ✅ 添加了新的模型或API接口

---

## 📋 重启步骤

### 1. 重启后端服务

**方法一：如果后端正在运行**
1. 找到运行后端的终端窗口
2. 按 `Ctrl + C` 停止服务
3. 重新启动：
```bash
cd backend
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**方法二：如果后端未运行**
```bash
cd "/Users/zhangyang/Library/Mobile Documents/com~apple~CloudDocs/2. 领域（Areas）/17 学习AI 与 编程/Code/CloudunCollectionONE/backend"
source venv/bin/activate  # 激活虚拟环境
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**预期输出**：
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [xxxxx] using WatchFiles
INFO:     Started server process [xxxxx]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

### 2. 重启前端服务

**方法一：如果前端正在运行**
1. 找到运行前端的终端窗口
2. 按 `Ctrl + C` 停止服务
3. 重新启动：
```bash
cd frontend
npm run dev
```

**方法二：如果前端未运行**
```bash
cd "/Users/zhangyang/Library/Mobile Documents/com~apple~CloudDocs/2. 领域（Areas）/17 学习AI 与 编程/Code/CloudunCollectionONE/frontend"
npm run dev
```

**预期输出**：
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

---

## ✅ 验证服务状态

### 验证后端
访问：http://localhost:8000/docs
- 应该能看到自动生成的API文档
- 检查是否有新的数据看板API端点（/api/v1/communications, /api/v1/performance等）

### 验证前端
访问：http://localhost:5173/
- 应该能正常登录
- 点击左侧菜单"数据看板" → "我的业绩看板"
- 应该能看到页面（即使数据为空）

---

## 🐛 常见问题

### Q1: 后端启动报错 "No module named 'xxx'"
**原因**：虚拟环境未激活或缺少依赖
**解决**：
```bash
cd backend
source venv/bin/activate
pip install -r requirements.txt
```

### Q2: 前端启动报错 "Cannot find module 'xxx'"
**原因**：npm包未安装
**解决**：
```bash
cd frontend
npm install
```

### Q3: 页面显示白屏或404
**原因**：前端路由未正确配置
**解决**：清除浏览器缓存，强制刷新（Cmd+Shift+R）

### Q4: API调用返回500错误
**原因**：数据库表未创建
**解决**：
```bash
cd backend
python create_dashboard_tables.py
python generate_dashboard_test_data.py
```

---

## 🎯 完整启动顺序（从零开始）

```bash
# 1. 激活后端虚拟环境并启动
cd "/Users/zhangyang/Library/Mobile Documents/com~apple~CloudDocs/2. 领域（Areas）/17 学习AI 与 编程/Code/CloudunCollectionONE/backend"
source venv/bin/activate
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 2. 新开终端窗口，启动前端（不要关闭后端）
cd "/Users/zhangyang/Library/Mobile Documents/com~apple~CloudDocs/2. 领域（Areas）/17 学习AI 与 编程/Code/CloudunCollectionONE/frontend"
npm run dev

# 3. 访问 http://localhost:5173/
```

---

## 📌 快捷命令

创建这两个脚本文件可以快速启动服务：

### backend/start.sh
```bash
#!/bin/bash
cd "$(dirname "$0")"
source venv/bin/activate
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### frontend/start.sh
```bash
#!/bin/bash
cd "$(dirname "$0")"
npm run dev
```

使用方法：
```bash
chmod +x backend/start.sh
chmod +x frontend/start.sh

# 启动
./backend/start.sh    # 终端1
./frontend/start.sh   # 终端2
```

