# Mock数据创建脚本说明

## 📂 可用脚本

### 1. 小组群和SPV管理员Mock数据
**文件**: `create_team_groups_mock_data.py`

**功能**:
- 创建2个小组群（高额案件组、普通案件组）
- 为每个小组群创建SPV管理员账号
- 更新现有小组，关联到小组群
- 确保所有小组都关联催收队列

**使用方法**:
```bash
cd backend
source venv/bin/activate
python create_team_groups_mock_data.py
```

**创建的数据**:
- 小组群: 2个
- SPV管理员: 2个（张三、李四）
- 密码: password123

---

### 2. 小组管理员Mock数据
**文件**: `create_team_admins_mock_data.py`

**功能**:
- 为每个小组创建3个不同角色的管理员
  - 主管 (supervisor)
  - 组长 (team_leader)
  - 质检员 (quality_inspector)

**使用方法**:
```bash
cd backend
source venv/bin/activate
python create_team_admins_mock_data.py
```

**创建的数据**:
- 每小组管理员: 3个
- 总计: 6个管理员（2个小组 × 3个角色）
- 密码: password123

---

## 🚀 快速开始

### 完整初始化流程

```bash
# 1. 进入后端目录
cd /Users/zhangyang/Documents/GitHub/CollectionONE/backend

# 2. 激活虚拟环境
source venv/bin/activate

# 3. 创建小组群和SPV管理员
python create_team_groups_mock_data.py

# 4. 创建小组管理员
python create_team_admins_mock_data.py

# 5. 重启后端服务
./restart_backend.sh
```

### 单独运行某个脚本

如果只需要创建特定的Mock数据：

```bash
# 只创建小组群数据
python create_team_groups_mock_data.py

# 只创建小组管理员数据
python create_team_admins_mock_data.py
```

---

## 📊 创建的账号汇总

### SPV管理员 (小组群长)
| 姓名 | 登录ID | 密码 | 所属小组群 |
|------|--------|------|-----------|
| 张三 | zhangsan | password123 | 高额案件组 |
| 李四 | lisi | password123 | 普通案件组 |

### 小组1管理员
| 姓名 | 登录ID | 密码 | 角色 |
|------|--------|------|------|
| 王五 | team1_supervisor | password123 | 主管 |
| 赵六 | team1_leader | password123 | 组长 |
| 孙七 | team1_inspector | password123 | 质检员 |

### 小组2管理员
| 姓名 | 登录ID | 密码 | 角色 |
|------|--------|------|------|
| 周八 | team2_supervisor | password123 | 主管 |
| 吴九 | team2_leader | password123 | 组长 |
| 郑十 | team2_inspector | password123 | 质检员 |

---

## ⚠️ 注意事项

1. **运行顺序**: 建议先运行小组群脚本，再运行小组管理员脚本
2. **数据库要求**: 确保数据库中已存在甲方、机构和催收队列数据
3. **重复运行**: 脚本会检查数据是否已存在，避免重复创建
4. **密码安全**: 所有账号的密码都是 `password123`，仅用于测试
5. **环境要求**: 需要在虚拟环境中运行

---

## 🔧 自定义修改

### 修改角色类型
在 `create_team_admins_mock_data.py` 中修改 `roles` 列表：

```python
roles = [
    {"role": "supervisor", "role_name": "主管", "suffix": "supervisor"},
    {"role": "team_leader", "role_name": "组长", "suffix": "leader"},
    {"role": "quality_inspector", "role_name": "质检员", "suffix": "inspector"}
]
```

### 修改小组群信息
在 `create_team_groups_mock_data.py` 中修改小组群创建部分：

```python
team_group_1 = TeamGroup(
    group_code="GROUP_HIGH_VALUE",
    group_name="高额案件组",
    group_name_en="High Value Cases Group",
    description="负责处理高额催收案件",
    # ... 其他字段
)
```

---

## 📖 相关文档

- [小组群Mock数据说明.md](../小组群Mock数据说明.md)
- [小组管理员Mock数据说明.md](../小组管理员Mock数据说明.md)
- [小组群功能说明.md](../小组群功能说明.md)

---

## 🐛 问题排查

### 问题1: ModuleNotFoundError
**解决**: 确保已激活虚拟环境
```bash
source venv/bin/activate
```

### 问题2: 数据库连接失败
**解决**: 检查数据库文件路径是否正确（默认为 `cco_test.db`）

### 问题3: NOT NULL constraint failed
**解决**: 确保已运行数据库结构更新脚本
```bash
./update_team_group_structure.sh
```

### 问题4: 重复数据
**解决**: 脚本会自动检查并提示，可以选择覆盖或跳过

---

## 📅 更新日期
2025年11月20日

